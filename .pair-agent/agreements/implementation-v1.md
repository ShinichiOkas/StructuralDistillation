---
sprint_id: 6ddf00eb-32f5-469d-b2e5-85acb583cfac
version: 2
status: completed
created_at: 2026-09-22T22:20:00+09:00
updated_at: 2026-09-22T23:20:00+09:00
domain_tags: [implementation, python-library, llm-judgment]
slice_size: M→L（コアの全層・触れる CLI・テスト・適合検査。受入は協議エンジン＝別コンテキスト）
change_count_premise: 0
change_count_improvement: 2
---

# スプリントゴール

実装設計 `doc/IMPLEMENTATION_DESIGN.md` v2 どおりにライブラリ v1（`structural_distillation`）を書き、
**測定 2 周目のキャッシュだけで `judge()` が記録を再現する**（適合検査）ところまで持っていく。

## 0. 師匠の言葉（原文・2026-09-22 22:18）

> 実装を開始してください

D1・D2 の読み（AI 判断・事後確認対象。`implementation-design.md` v3 に記録）: 標準ライブラリの Ollama アダプタで進め LLMProviderlib は読まない／実装は 1 スプリントに畳み、層ごとの完成検査をコミットで残し、CLI を触る人間ゲートは最後にまとめて提示する。

## タスク（内側から一層ずつ。各層の完成 ＝ テスト緑・触れる CLI・設計と一致。層ごとにコミット）

- [x] A0 足場: `pyproject.toml`・`.venv`・`contracts.py`・`prompts.py` ＋ `prompt_sets/ja/set.json`（空撃ちの指示を逐語で）
- [x] A1 L0: `l0.py` ＋ `test_l0.py`（鍵が out4 のキャッシュ行と一致）＋ `tools/l0_chat.py` ＋ 実接続 1 回（クラウド）
- [x] A2 単位化: `units.py` ＋ `test_units.py` ＋ `tools/units_chat.py`
- [x] B L1: `l1.py` ＋ `test_l1.py` ＋ `tools/l1_chat.py`
- [x] C1 L2: `l2.py` ＋ `test_l2.py` ＋ `tools/l2_chat.py`
- [x] C2 L3: `l3.py` ＋ `test_l3.py`（S0a 適合・S4・S12）＋ `tools/l3_chat.py`
- [x] C3 L4: `l4.py` ＋ `test_l4.py` ＋ `tools/l4_chat.py`
- [x] D 合成: `compose.py`（受入 M4 で `judge.py` から改名）＋ `test_judge.py` ＋ `tools/judge_cli.py` ＋ `tools/conformance_out4.py`（①②③）
- [x] E 実接続の端から端まで（クラウドモデルだけ）: 新しい本文 2 本。記録は `.pair-agent/probes/impl-v1-live/`
- [x] F 設計 v3 / v3.1・README
- [x] G 受入: 協議エンジン（critical 0・major 5・minor 16）。すべて処理（下の表）
- [x] H 師匠に CLI を触ってもらった（`judge_cli` で灯台守・出力の読み方を伺われた）・M5 の札は師匠決定（計器不良＋理由＋材料。f9554c9d）・振り返り済（2026-09-23。5 スプリントまとめて）

## スコープ

`structural_distillation/`・`tools/`・`tests/`・`pyproject.toml`・設計文書と README の書き戻し。
`.pair-agent/probes/` の道具と記録は**読むだけ**（凍結。テストも道具も書き込まない。道具の書き込み先は `_cli.guard_write_path` が拒む）。
実接続の記録は走行後に `.pair-agent/probes/impl-v1-live/` へ写した（道具からではなく取り込み）。
LLM を呼ぶのはクラウドモデル（`-cloud`）だけ。ローカルモデル（GPU）は呼んでいない。

## 完了条件

| 条件 | 判定 | 根拠 |
|---|---|---|
| `pytest`（既定・LLM を呼ばない）が全部緑。skip は理由つきで数を報告 | ✅ | 161 緑・1 skip（実接続。理由「SD_LIVE=1 と SD_LIVE_MODEL を与えたときだけ走る」を `-rs` で表示） |
| 適合検査 ①: base21 のキャッシュだけで 21 題材の主走行と反事実が外れ 0 で一致 | ✅ | 21/21・外れ 0・命中 1,941 |
| 適合検査 ②: base21 ＋ xc2 で交差検証あり、外した・弱・非排他が一致 | ✅（範囲つき） | 21/21・外れ 0・命中 2,289。⚠ この 21 題材は外される軸が 0（受入 M1）。外す経路は ③ が見る |
| （追加）適合検査 ③: 軸を外す経路 | ✅ | 弱い生成器の軸 11 題材で 11/11 一致・外した 5 軸（4 題材）・外す前と後の p・w が 3 体とも一致・外れ 0 |
| （追加）陰性対照 | ✅ | 回答の版を変えると ① 0/21・② 0/21・③ 0/11 |
| コアの import が標準ライブラリだけ（AST 走査） | ✅ | `test_core_imports_only_the_standard_library` |
| 各層に触れる CLI があり、1 回ずつ動かした出力を記録した | ✅ | 下の「層ごとの完成の記録」と `.pair-agent/probes/impl-v1-live/README.md`（judge_cli） |
| 実接続（クラウド）で 1 題材を端から端まで通した記録がある | ✅ | 2 題材。`.pair-agent/probes/impl-v1-live/`（記録・キャッシュ・コマンド・結果） |
| 受入で critical 0 | ✅ | critical 0。major 5・minor 16 はすべて処理 |

## 前提

- 実装設計 v2・上流設計 v3.5 を正とする。実装で崩れたら「前提崩壊」として数え、設計を直す（**前提崩壊 0**）
- ライブラリは空撃ちの移植。差分は設計 §13 に全部載せる。載っていない差分が見つかったら、直すか表に足す（受入で 5 つ見つかり P14〜P18 に足した）

## 既知リスク

- 指示の逐語移植が 1 文字ずれると鍵が合わない → 鍵の一致テストと適合検査が検出する（変異試験で確認）
- Ollama は師匠の常駐物（0.34.1）。止めない・再起動しない（止めていない）
- クラウドの 429 / 5xx → アダプタが待って再送（実接続の間に起きなかった）

## 実行中に採る判断（AI の判断。事後確認対象）

- 協議エンジンの評価は設計 v1 で済（critical 2・major 13 を反映して v2）。**実装合意は設計の写しなので評価を省き、受入（G）で実装と設計の突き合わせに回した**
- 設計の記述が実装で曖昧と分かったら、空撃ちの挙動に合わせて決め、設計 v3 と差分表に書いた
- S0a の札の比較は走行時の閾値（κ=0.5）で行う（記録の札はその値で付いている。mono の m17 の 1 行が κ 0.5〜0.667 の帯にある）
- 受入 M5（全軸が外れたときの札）は、上流 F4 どおり NO_EVIDENCE のまま、率を `None`・注記で原因を区別する形で仮置きし、師匠に伺う
- 受入の「検証役の既定」（m14）は挙動を変えず README を直した（測定で生成器自身を検証役にしても検出数は同等だったため）
- 受入の「適合検査に外す経路を足すか」（M1）は、主張していた検査を主張どおりにする手当てなので、伺わずに足した

## 層ごとの完成の記録

| 層 | テスト | 触れる CLI（動かした結果） | 設計との差 |
|---|---|---|---|
| A0・A1 L0・A2 単位化 | `test_l0` 29・`test_units` 6 緑（受入後 `test_l0` 40）。**鍵が out4 の実キャッシュ行（p3 1・p2 2・xc2 3）と一致**＝指示の逐語移植を固定。実接続 1 回（gemma4:31b-cloud）緑 | `units_chat`: 3 単位に割れる。`l0_chat --fake all_no`: 否定している＋ s1。`l0_chat --model gemma4:31b-cloud --cache`: 同じ行の 2 回目はキャッシュ命中 | ① スキーマの持ち主は `prompts.py`（語が指示の集合から来るので。設計は l1 / l2 と書いていた）② `structured` もフェンスを剥がす（利用側の Reader が剥がさなくても通る。入れ子のフェンスは空撃ちと違う＝P17）③ パッケージの公開名は遅延読み込み（層を 1 つずつ作ってテストするため） |
| B L1 | `test_l1` 19 緑（受入後 20）（規則を 1 件ずつ破る・試行・交差検証の過半数／同数／weak／nonexclusive／検証役 1 体／票の失敗） | `l1_chat` m01 を測定のキャッシュだけで: **実 0・外れ 0**、外した／弱／非排他が `crosscheck.json` と一致（[] / a01 a03 a04 / []）。新しい本文（佐藤の資料・「佐藤は不誠実である」）で実接続: 生成 1・交差検証 60、67 秒。a01 の反証側「共同名義**または**他者の名義で提出した」を検証役 2 体とも支持側と読み、外した | ④ 同一の両側は空撃ちと同じく H4 にも当たる（テストで固定） |
| C1 L2 | `test_l2` 7 緑（正規化・照合・触れていないは根拠不要・再送 → 無効・evidence 欠落は無効・並列でも順序固定・鍵の記録） | `l2_chat` 新しい本文・qwen3.5:397b-cloud で実接続: 「名前を出さなかった」→ 述べている [s3]、「すべて自分で作った」→ 否定している [s2]、「上司に叱られた」→ 否定している [s3]（褒められた） | ⑤ `reinterpret`（記録の生応答から引き直す）を足した（replay の reparse 用） |
| C2 L3 | `test_l3` 32 緑（受入後 34）。**S0a: 測定 2 周目の 8 腕・主走行と反事実の全 465 行**（腕ごとの行数もテストで固定）を引き直し、p・w・s・r・n・札（走行時 κ=0.5）・軸ごとの d・d_samples・一致率・矛盾・無効・沈黙、主走行の率と標本ごとの p が全一致。S4（側の入れ替えで p' = 1 − p、300 通り）・S12（軸の複製で p・w・札・率が不変、300 通り） | `l3_chat --probe base21 --id m01`: 実読み手 2 体 p 0.50・割れる、偽読み手 2 種は矛盾率 1.00 で計器不良（記録の札と一致） | ⑥ 設計 §4.3 の「矛盾率が [0.5, 0.667) の行は無い」は誤り（mono の m17 に 1 行）。S0a は走行時の閾値で比べる |
| C3 L4 | `test_l4` 21 緑（受入後 23）（K 等分は空撃ちの式・切れ目・ラベル・値なし・2,000 通りで型の外に出ない・不正な型は InputError・[0, 1] の外の p はプログラムの誤り） | `l4_chat --ordinal 5`: 0→1・0.2→2・0.5→3・1→5。切れ目 0.3 / 0.7: 0.29→低・0.3→中・0.7→高。`--label NO_EVIDENCE` → 値なし | ⑦ 型の検査 `check_output` と、札を見ない `type_value`（代表値用）を L4 に置いた |
| D 合成 | `test_judge` 22 緑（受入後 31）（入口の検査 F1・F2・重複名・偽の生成器／PlanningFailed／偽読み手 3 種で S11／既定の生成器と検証役／外した軸は答えも集約もしない／問いの集合を渡すと L1 を飛ばす／S8 呼び出し数とキャッシュ／標本と並列／記録 → replay（閾値・型・生応答からの引き直し）／judge_sync／単読み手／Q6／閾値の値域／検証役と軸 id の重複／全軸が外れたとき／import の順） | **適合検査 ①②③**（上の完了条件）。`judge_cli` の実接続 2 題材（`.pair-agent/probes/impl-v1-live/README.md`） | ⑧ `replay(reparse=True)` を足した。検証役に偽読み手と重複名を拒む検査を足した |

### 変異試験（緑が偽物でないか）

`tools/mutation_check.py`: 実装を 1 箇所ずつ壊す。初回 23 通りで **21 殺・2 生き残り**:
H3 の閾値を 0.6 → 0.99 にしても落ちない（テストが完全一致の言い換えしか見ていなかった）／judge が交差検証で外した軸も集約しても落ちない（外れる軸がある judge のテストが無かった）。
テストを 2 本足して 23 殺。受入の修正に合わせて 5 通り足し、**28 通りすべて殺**。

## 受入（協議エンジン・2026-09-22 23:00）の指摘と処理

評価者はシェルが使えず、実行による確認はしていない（静的に数えた）。主張は AI が実行して確かめた（M3・M4 は再現した）。

| # | severity | 指摘（要旨） | 確認 | 処理 |
|---|---|---|---|---|
| M1 | major | 適合検査 ② は軸を外す経路を実データで通していない（21 題材とも flagged 空） | 成立 | ③ を足した（弱い生成器の軸で 5 軸を外す）。完了条件と §14 の表現を範囲つきに直した。陰性対照で見つかった「値が未定義どうしの一致」を外れの検出で落とすよう直した |
| M2 | major | 差分表に無い空撃ちとの差（生成・交差検証の validate／回答の再送／外した後の母数／フェンスの 2 回剥がし） | 成立 | 設計 §13 P14〜P18（失敗の行 m1 を P18 に）。P3・P6 の文言を直し、「変えていないこと」に条件を付けた |
| M3 | major | ρ ≤ 0 で `label` が AssertionError | 成立（再現した） | s + r = 0 は ρ に関わらず NO_EVIDENCE。閾値の値域を入口と replay で InputError。テストと変異で固定 |
| M4 | major | 公開名 `judge` とサブモジュール `judge.py` の衝突（import の順で judge がモジュールに化ける） | 成立（再現した） | `compose.py` に改名。別プロセスで import の順を変えて確かめるテスト。`__init__` に注記 |
| M5 | major | 全軸が外れると札が「本文に根拠が無い」で率 0.0 | 成立（上流 F4 どおりの挙動） | 率を `None`、`Reading.note` で原因を区別（F4′）。**札をどうするかは師匠に伺う**（下） |
| m1 | minor | 既存キャッシュの ok:false 行を当たりにする | 成立 | 読み込み時に読み飛ばす（P18）。テスト・変異 |
| m2 | minor | 合流の端（追記の失敗で待つ側に CancelledError）・設計の asyncio.Lock の記述 | 成立 | 待つ側を先に放し、追記の失敗はログにして続ける。設計 §7 の記述を直した |
| m3 | minor | 失敗した回答の raw と key が別の試行の組になりうる | 成立 | 試行ごとに組にする。テスト・変異 |
| m4 | minor | 入口の検査の漏れ（検証役の重複・軸数が範囲外・渡した問いの集合の重複） | 成立 | InputError。テスト |
| m5・m6 | minor | 設計の記述と実装の食い違い・実装にあって設計に無いもの | 成立 | 設計 §2・§3・§4.0・§4.1・§4.3・§5・§8・§9 を直した |
| m7 | minor | 指示の集合の検査が薄い（語の一意性・差し込み口）・replay の自前の集合 | 成立 | 読み込み時に検査。replay は組み込みに無い集合なら InputError、digest が違えば警告。テスト・変異 |
| m8 | minor | K 等分の浮動小数の境界・[0, 1] の外の p | 成立 | 空撃ちと同じ式を保つ（注記）。範囲外は ValueError |
| m9 | minor | テストの主張の粒度（465 を固定していない・件数表の誤り・random の並列・nonexclusive の None・S12 の沈黙率・S2 の件数） | 成立 | それぞれテストを足し、件数表を直した |
| m10 | minor | 適合検査の道具の弱いところ（PlanningFailed で止まる・陰性対照のオプションが無い・「実 0」は構造的） | 成立 | 題材ごとに捕まえて報告、`--negative-control`、指標を「外れ 0」と明記 |
| m11 | minor | 実接続のテストが skip でなく deselect | 成立 | addopts を `-rs` に。理由つきの skip として出る |
| m12 | minor | §12 に載っていない日本語依存 | 成立 | 設計 §12 に追記（「」・全角空白・違反の文言） |
| m13 | minor | 道具の書き込み先に測定の記録を拒む防御が無い | 成立 | `_cli.guard_write_path`。確かめた（judge_cli で拒否） |
| m14 | minor | README の交差検証の説明が挙動と違う | 成立 | README を直した（挙動は変えない。上の判断） |
| m15 | minor | 合意・ジャーナルの進行が止まっている | 成立 | この版で更新 |
| m16 | minor | 差分表の文言 | 成立 | M2 で処理 |

却下した指摘: なし。

## 師匠に伺うこと

- **受入 M5: 交差検証で全軸が外れたとき（または渡した問いの集合に有効な軸が無いとき）の札。** (a) 今の仮置き（札は「本文に根拠が無い」、率は無し、注記で原因を区別）／(b)「計器不良」にする／(c) 問いの生成の失敗（PlanningFailed）と同じく拒否する
- 前スプリントから持ち越し: 振り返り（測定 2 周目・実装設計・実装 v1）、C-3（弱い単一系統に GPU を使うか）、D1（LLMProviderlib のアダプタを足すために読む許可）

## 協議ログ

- v1 [2026-09-22 22:20] 起草
- v2 [2026-09-22 23:20] 実装 A〜F 完了、受入（critical 0・major 5・minor 16）を全件処理

## 変更ログ

- v2 [2026-09-22T23:20:00+09:00]: 実装完了・受入の処理。**より良い方法 2**（適合検査 ③ を足した／合成を compose.py に改名）。前提崩壊 0。status → retrospecting
- v1 [2026-09-22T22:20:00+09:00]: 初版
