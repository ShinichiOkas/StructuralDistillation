---
sprint_id: 49478e2b-819b-464a-b1cf-2098678011e5
version: 1
status: drafting
created_at: 2026-09-21T21:05:20+09:00
updated_at: 2026-09-21T21:05:20+09:00
domain_tags: [upstream-design, llm-judgment, library-design]
slice_size: M（合意ドキュメント＋上流設計文書。実装なし）
change_count_premise: 0
change_count_improvement: 0
---

# スプリントゴール

本プロジェクトのコンセプトを理解して**上流設計を固める**。
当面は LLM を用いて判断の精度を上げることが目的。専用モデル（蒸留）は先の段階。

## 0. 師匠の言葉（原文・2026-09-21）

依頼:

> 本プロジェクトのコンセプトを理解して上流設計を固めてほしい。基本的には専用モデルを作るのは先の段階として、
> 当面はLLMを用いて判断の精度を上げることが目的。

コンセプトと規則:

> このプロジェクトは、入力した命題に対して客観的判定を行うこと。そして判定出力（スカラー）を適切な分布の連続量ないし離散値として得られること。
> その方法として内部的には対立を含む複数の問いに対してLLMで答えさせ、その分布から出力値を作るというコンセプト。
> それが実現できる汎用ライブラリであること。
> 基本的なロジックの設計は他のプロジェクトを参照せずに行うこと。実際の生きたデータを用いて何か実験したいなどの場合において他のプロジェクトを参照してもよい。

例:

> 例えば「羅生門」において「老婆は悪人である」の度合いを１～５で示せ。というような命題に対して答えが出ること。

決定（AI の 3 つの問いへの回答）:

> pair-agentの初期化はしてよい。実装言語はPython。
> 羅生門は何をしたいかを明確に伝えるための例であり、基本的には判定に必要な全文を入力する（LLMの知識に頼らない）。
> 問いの集合もLLMが生成することを基本と考える。よって問いの生成ルールやそれを規定するハーネスもこのプロジェクトに含まれる。
> 適切な分布は(b) 一つの出力が幅を伴って返ることを示す。
> その分布の確からしさ、コンフィデンスレベルも添えられればなおよい。
> 対立を含むは、LLMの偏りを打ち消すことを目的とする。Y,N両方の観点から問い、Y,Nが割れるのか、偏るのかを判定する。なので軸の撮り方は命題次第。

## タスク

- [ ] T1 合意ドキュメント（本文書）を起草し、協議エンジンの評価を反映する（担当: AI）
- [ ] T2 上流設計 `doc/UPSTREAM_DESIGN.md` v1 を起草する（担当: AI）
- [ ] T3 協議エンジン（deliberation）に T1・T2 を評価させ、指摘を反映して v2 にする（担当: AI）
- [ ] T4 師匠に「未決事項だけ」を提示し、判断を得る（担当: 両方）
- [ ] T5 判断を反映し、設計 → 評価 → 精緻化を師匠が「足りる」と言うまで回す（担当: 両方）
- [ ] T6 README の「まだ決まっていないこと」を、決まった分だけ更新する（担当: AI）
- [ ] T7 振り返り（担当: 両方）

## スコープ

- **文書だけ。実装はしない。** コード・テスト・パッケージ構成の確定はこのスプリントの外
- 材料はこのリポジトリ（README・`doc/`）と師匠の言葉。**他のプロジェクトは読まない**（師匠の規則。実験の段になったら改めて伺う）
- 上流設計の範囲: 目的・品質特性・入出力契約・内部の変換（層）・問い生成ハーネスの規則・
  分布と出力値の作り方・品質シナリオ・解かないこと・未決事項
- 蒸留（レイヤー 3）は「記録の形が蒸留の素材になる」ことだけ押さえ、中身は設計しない

## 完了条件

- 上流設計 `doc/UPSTREAM_DESIGN.md` が存在し、次を含む: 品質特性 / 入出力契約 / 層ごとの IN→OUT /
  問い生成ハーネスの規則表（各規則に検査方法） / 集約規則 / 品質シナリオ（各シナリオに応答尺度） / 未決事項
- 設計中の各要求が、師匠の言葉（§0）・README・`doc/LESSONS_FROM_THE_FIRST_CONSUMER.md` のいずれかに辿れる
  （辿れないものは「AI 判断」と明記されている）
- 協議エンジンの評価を 1 回以上通し、critical 級の指摘が残っていない
- README の「まだ決まっていないこと」が、決まった項目については更新されている
- 師匠が「足りる」と言った

## 前提

- 実装言語は Python
- 当面はレイヤー 1・2（問い生成・回答・集約）。蒸留は後段
- 判定に必要な全文を入力する。LLM の知識に頼らない
- 問いの集合は LLM が生成する。生成ルールとハーネスは本プロジェクトの範囲
- 1 つの出力は幅（分布）を伴って返る。確からしさ（コンフィデンス）は添えられればなおよい
- 対立を含む問い＝Y/N 両方の観点から問い、答えが割れるか偏るかを判定する。軸の取り方は命題次第
- README の設計上の約束（依存は一方向・ドメイン知識は利用側・重みは利用側・テストは単体で走る）は維持する

## 実行中に採る判断（AI の判断。事後確認対象）

| # | 判断 | 採る側 | 根拠 |
|---|---|---|---|
| J1 | 回答は 3 値（Yes / No / 判定不能）。判定不能を選べる | 採る | 選択肢を与えると LLM は必ずどれかを選ぶ。判定不能が無いと根拠の無い答えが混ざる（README「向いていないこと」の精神） |
| J2 | Yes / No には本文からの逐語引用を添えさせ、引用の実在をコードで照合する。無い答えは無効 | 採る | 「判断そのものの正しさは別問題」（README）。判断は測れなくても、根拠の捏造は測れる |
| J3 | 問いの向きを対称に扱う: 支持観点の問いの Yes は支持、No は反証。反証観点はその逆 | 採る | 「Y,N が割れるのか、偏るのか」を数えられる形にする。両観点同数なら一律の Yes 偏りは差で打ち消える。品質シナリオ S4（相補性）で検証する |
| J4 | 度合いの分布は、支持軸数と反証軸数から Beta 分布（一様事前）で作る | 採る | 幅が軸数に応じて自然に決まり、決定論で再計算できる。他の作り方は実験で比べる |
| J5 | 順序尺度（1〜5）の値は分布の中央値が落ちる段 | 採る | 最頻値より外れ値に強い。実験で比べる |
| J6 | 確からしさは LLM に申告させない。有効軸の割合・読み手間の一致率・引用捏造率など数えられる量から作る | 採る | 師匠のコンセプト（分布から出力値を作る）と同じ向き。合成式は未較正のまま「診断値」として並べて返す |
| J7 | 問いの集合は生成器（1 モデル）で 1 回作って固定し、生成器の名前と指示の版を記録する | 採る | 読み手を替えたときの差を、問いの差と答えの差に分けて測れるようにする |
| J8 | 生の LLM 応答は入力の鍵付きで追記のみのキャッシュに残し、集約は再計算で行う | 採る | 検証可能性（README 問題 3）と、テストを単体で回すため |
| J9 | 生成指示・回答指示（プロンプト）は版付きの外部ファイル。規則の検査はコード | 採る | 用途で変わるものは外へ、形を守るものはコアに（README 約束 2） |
| J10 | パッケージ名は `structural_distillation`、公開 API は `judge(...)` 1 本から始める | 採る（仮） | README 未決事項。最小から始める |

## 既知リスク

- 「対立を含む」の私の読み（§0 の師匠の言葉 → 設計 §6）がずれていると、ハーネスの規則ごと組み直しになる
- 問いの集合が生成器のモデルで変わる。読み手非依存の主張が「問い生成」の段で崩れうる（設計 S3 で測る）
- 確からしさの合成式は未較正。「較正されている保証は無い」（README）のまま出す
- 本文が 1 回の文脈に入らない長さの場合は v1 の範囲外（未決 U2）

## 変更ログ

- v1 [2026-09-21T21:05:20+09:00]: 初版（師匠の言葉・タスク・前提・AI 判断 J1〜J10）
