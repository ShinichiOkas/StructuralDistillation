---
sprint_id: bb3c8a3c-05a2-49aa-87d9-27e70bc431bd
version: 3
status: retrospecting
created_at: 2026-09-22T17:45:00+09:00
updated_at: 2026-09-22T22:20:00+09:00
domain_tags: [implementation-design, python-library, llm-judgment]
slice_size: M（合意ドキュメント＋実装設計文書 `doc/IMPLEMENTATION_DESIGN.md`。協議エンジンの評価＝別コンテキスト。コードは書かない。実装は次のスプリント群）
change_count_premise: 1
change_count_improvement: 0
---

# スプリントゴール

上流設計を「どう組むか」に落とし、**ライブラリを書き始められる状態**にする。
配置・契約（型）・各層の IN → OUT と内部規則・指示の外部化・記録とキャッシュ・実行モデル・失敗の型・テスト・実装の順序・空撃ちとの差分が
`doc/IMPLEMENTATION_DESIGN.md` に書かれ、協議エンジンの critical が 0 で、師匠の決定が入っていること。

## 0. 師匠の言葉（原文・2026-09-22）

> 実装設計に進んでください

前スプリントまでの規則（在効。原文は `upstream-design.md` §0・`measurement-2.md` §0）:

> 実装言語はPython。
> 基本的なロジックの設計は他のプロジェクトを参照せずに行うこと。実際の生きたデータを用いて何か実験したいなどの場合において他のプロジェクトを参照してもよい。
> 1.値は全て仮設。動くのは当然。汎用的な値に落とす事が方針 2.僕と同じ感覚にすることは目的ではない。逆に僕の感覚を取り入れるべきではない 3.とくに違和感はないが、僕の違和感は校正ではない。僕に問う意味はない
> すまないがcloudモデルで実行する方針に切り替えてくれ。ollamaのモデルで-cloudがついているもの。これはGPUを使わない

## 過去の仕掛との関係（起草前に確認）

| 仕掛 | 関係 | 扱い |
|---|---|---|
| 前スプリント「測定 2 周目」の振り返り（未実施）と C-3（弱い単一系統に GPU を使うか。未回答） | 師匠が次へ進んだので先送り | このスプリントは GPU を使わない（実接続の検査はクラウドモデル）。振り返りは D2 で伺う。C-3 は答えが来るまで「弱い単一系統は未測定」のまま（上流 §10） |
| 上流設計 §13 の次の測定 5 件（検出項目・同義対・ブループリント・分散成分・羅生門のやり直し） | 実装設計が記録の形を固定すると、後から載せにくくなる | 記録に `schema_version` と `Axis.kind`・`QuestionSet.source` を最初から置く（設計 I12）。測定そのものは含まない |
| README「まだ決まっていないこと」: パッケージ名・API の形 | このスプリントで決まる | 設計 §2・§4.5（`structural_distillation` / `judge` / `judge_sync` / `replay`）。README は T3 で更新 |
| README「まだ決まっていないこと」: 蒸留の学習基盤 | 決めない | 変更なし |
| README「まだ決まっていないこと」: 交差検証と本文についての命題の手当て | 測定 2 周目で済（H12 既定 ON・S14 不採用） | README の行を T3 で更新 |
| `doc/structural_distillation.md` の二重管理（上流 §12 保留） | 触らない | 変更なし |
| ペア固有 Skill `ai-dev-basics`（新規プロジェクトは LLMProviderlib を使う・ストリーミング既定 ON・非同期既定・実 API テストは `@pytest.mark.integration`） | 師匠の規則「他のプロジェクトを読まない」と衝突する（LLMProviderlib は師匠の別プロジェクト） | 非同期と `integration` マーカーは従う（I2・§9）。ストリーミングは使わない（構造化出力に意味が無い。I2）。LLMProviderlib は読まずに v1 を組み、接続は `Reader` の口で差し替え可能にする（I1）。**D1 で伺う** |

## 前提の確認（協議エンジンが実ファイルで確かめた。第三者が同じ集計で再現できる）

| 前提 | 結果 | 出所 |
|---|---|---|
| `out4/base21/llm_cache.jsonl` は生成器（p3）と回答（p2）の応答を含む | 成立。1,941 行 ＝ p3 21 ＋ p2 1,920。`p2:retry` 0・`ok:false` 0 | 版ごとの行数集計 |
| out4 の全腕に失敗応答（`ok:false`）が無い | 成立。全 10 腕で 0 | 同上 |
| 交差検証（xc2）のキャッシュ | base21 には無い。`out4/xc2/llm_cache.jsonl` 1,920 行（base21 の 126 軸分 ＋ 弱い生成器の 66 軸分）と `out4/xc_self/` 960 行 | 同上 |
| 空撃ちは標準ライブラリだけ | 成立 | `probe.py` の import 行 |
| 偽読み手は `answer()` で短絡し Port（キャッシュ・カウンタ）を通らない | 成立 | `probe.py` `answer` 冒頭 |
| キャッシュ行の形 | 成立。ただし失敗時の payload は `{ok, content, error}` だけ | `probe.py` `Port.chat` |
| 適合検査の条件（生成器 gemma4:31b-cloud・読み手 qwen3.5:397b-cloud / glm-5.2:cloud・標本 2・軸 6・num_ctx 8192・交差検証なし） | 成立。走行には偽読み手 2 種も混ざっている（適合検査では任意） | `out4/base21/run.log` 先頭行 |
| **無効な回答の向きの規則** | **上流 §5.3（無効を含む → d = 0）と空撃ち（無効はその側の証拠なし）が違う。** 主走行の 8 腕に「d ≠ 0 かつ無効」の標本は 0、反事実の行に 7 | `out4/*/results.json` の `v_support` / `v_refute` / `d_samples` を走査 |
| 呼び出しの規模 | out4 のキャッシュ 15,614 行（腕をまたぐ重複を含む）、空撃ち 1〜2 周目（`out/`・`out3/`）2,379 行 | 行数 |

## タスク

- [x] T1 実装設計 v1 を書く（担当: AI）
- [x] T2 協議エンジンに合意と設計を評価させ、成立する指摘を反映して v2 にする（担当: AI）
- [x] T3 師匠の決定 D1・D2 を受け、設計 v3・README「決まったこと／まだ決まっていないこと」を更新する（担当: 両方）→ 師匠「実装を開始してください」。設計 v3 と README は実装スプリントの中で、実装と突き合わせてから更新する（書いたとおりに作れたかを見てから「決まった」と書く）
- [x] T4 実装スプリントを起票する（6ddf00eb。1 スプリントに畳む）。このスプリントは retrospecting にし、振り返りは前スプリント分と合わせて実装の後に伺う
- [ ] T5 振り返り（前スプリントの先送り分と合わせるかは D2）（担当: 両方）

## スコープ

文書だけ。**コードは書かない**（テストの雛形も書かない）。
空撃ちの記録（`out4/`）と道具を読んで、適合検査（測定 2 周目のキャッシュだけで `judge()` を再現する）の前提が成り立つかを確かめることは含む（済。上の表）。
実装は次のスプリント群（設計 §10 の切片 A〜D）。

## 完了条件

- 上流設計 §4（契約）・§5（層）・§6（H1〜H12）・§7（式と札）・§8（記録）・§4.4（F1〜F7）の各項目が、実装設計の**付録 A の対応表**に載っている（「該当なし（理由）」の行を含む。第三者が表で照合できる）
- 空撃ちと違える箇所の一覧（設計 §13 差分表: 何を・空撃ち・ライブラリ・種別（置けない／意図的／追加）・理由）がある
- 協議エンジンの評価で critical 0（成立した指摘はすべて反映済み。却下は理由つきで協議ログに残る）
- 実装判断 I1〜I21 を師匠に提示し、異議があれば反映した（確定を求めない。事後確認対象）
- 師匠の決定 D1・D2 が本ドキュメントに原文で記録された
- README を更新した: 「決まったこと」にパッケージ名・API（`judge` / `judge_sync` / `replay`）・配置（1 層 1 ファイル）・実装の順序を足す。「まだ決まっていないこと」からパッケージ名・API・交差検証と本文についての命題の行を消す（蒸留の学習基盤は残す）
- 適合検査の前提が確かめられ、結果が本ドキュメントの表に残っている（済）

## 前提

- 上流設計は動かさない。実装設計で上流に矛盾が見つかったら「前提崩壊」として数え、上流を直す（**1 回発生: §5.3 の無効の扱い → v3.5 脚注**）
- 空撃ちの道具（`probe.py` / `probe3.py` / `probe4_run.py` / `probe4_crosscheck.py`）は動いた実物。ライブラリはその**移植**で、挙動を変える箇所は設計 §13 の差分表に載せて再測定の対象にする。probe*.py は今後編集しない（凍結）
- コアは標準ライブラリだけで書ける（空撃ちが証明した）
- 手元の Python は 3.13。`requires-python >= 3.11`

## 既知リスク

- 指示のテンプレート化（`$placeholder`）で空撃ちの文面と 1 文字でもずれると鍵が合わず、適合検査が「live 0 件」で落ちる → **適合検査そのものがこれを検出する**。落ちたら指示の差分を直す（設計を変えない）。鍵を同一にする要素は設計 §4.0 の表に列挙した
- 率の定義（設計 I4）が上流 §7.4 の表記（軸ベースの u₂/n）と違う。閾値はこの定義で置かれたので v1 は空撃ちの定義を採る（上流に脚注済み）
- 交差検証で外した軸に答えさせない（I6）は、測定 2 周目（答えてから外した）と手順が違う。1 記述 1 呼び出しなので回答行列は同じ、という論証に依っている。適合検査 ② で確かめる
- 非同期を本体にすると、同期でしか呼べない利用側（既に走っているイベントループの中）で `judge_sync` が使えない。文書に明記した
- 適合検査の道具が `.pair-agent/probes/` の記録に追記する事故 → `read_only` と「書かない」規則を設計 §9 に明記

## 師匠の決定（原文・2026-09-22 22:18）

> 実装を開始してください

D1・D2 の選択は言葉に無い。**AI の読み（事後確認対象）**:

- D1 → 推奨の既定で進める。v1 は標準ライブラリの Ollama アダプタ。LLMProviderlib は読まない（許可は出ていないので (b) 相当として扱い、v1 が動いてから改めて伺う）
- D2 → 「開始」の指示で、師匠はこのセッションを自律で進める形を採っている（途中で問いに答えられない）。そこで**実装を 1 スプリントに畳み（(b)）、層ごとの完成の検査（テスト緑・触れる CLI・文書との一致）は層ごとのコミットとして残す**。CLI を触る人間ゲートは、全層ぶんを最後にまとめて提示する。切片ごとに止まる (a) を望むなら、次からそうする
- 実装判断 I1〜I21 には異議が無かったものとして進める（事後確認対象のまま）

## 師匠に決めてもらうこと（D）

- **D1 LLMProviderlib のアダプタ。** v1 は標準ライブラリだけの Ollama アダプタで進め、他の接続は利用側が `Reader` を実装する（規則に触れない。推奨）。LLMProviderlib のアダプタを足すために同リポジトリを読む許可を、(a) 今出す／(b) 後で出す（v1 が動いてから）／(c) 出さない
- **D2 進め方とゲート。** (a) 切片 1 つ ＝ スプリント 1 つ（A〜D の 4 本。各層の CLI を師匠が触る人間ゲートを切片の終わりに。振り返りは前スプリント分と合わせてこの合意の後に 1 回）（推奨）／(b) 1 スプリント（M〜L）に畳み、層ごとのゲートは残す／(c) 別の形

篩にかけて外したもの（協議エンジンの判定に従う）: 実装判断 I1〜I21 は「確定でよいか」と問わず、事後確認対象として提示するだけ（異議があれば直す）。
I9（K 等分）・I10（代表値 ＝ 平均）は上流で確定済み（師匠「確定でよい」）なので蒸し返さない。I16（`max_chars` 12,000）は値なので問わない（実測で置き換える）。

## 実行中に採る判断（AI の判断。事後確認対象）

- 協議エンジンの指摘は、根拠資料（上流設計・空撃ちのコードと記録）で**成立するか確かめてから**反映する。成立しないものは理由つきで却下として協議ログに残す
- 協議エンジンが出す「師匠への問い」は 2 つの篩にかける: 師匠の主観・感覚を問うもの（規則: 問う意味はない）と、実装の既定で足りるもの。残ったものだけ D に上げる
- 人間ゲート（各層の CLI を触る）で問うのは**仕組みの決定**（次へ進むか・何を直すか）だけ。出力の値への感覚は問わない
- 設計文書の版は、協議エンジン反映で v2、師匠の決定反映で v3

## 協議ログ

- v1 [2026-09-22 18:05] 起草。上流設計 v3.4 と空撃ちの道具を読み直し、設計 v1（§0〜§12・I1〜I17）を書いた
- v2 [2026-09-22 18:40] 協議エンジンの評価（critical 2・major 13・minor 18・詰め不足スコア 5）。全件を根拠で確かめて処理:

| # | severity | 指摘（要旨） | 確認 | 処理 |
|---|---|---|---|---|
| C1 | critical | 無効な回答の向きの規則が空撃ちと違う（設計「d は変わらない」は偽）。上流 §5.3 とも食い違う | 成立。反事実の行に「d ≠ 0 かつ無効」7 標本を実データで確認 | 空撃ちの規則に揃える（設計 §4.3）。上流 v3.5 に脚注（**前提崩壊 1**）。S0a の検査対象に `d_samples`・`agree`・`p_by_sample` 等と反事実の行を含める。差分表に記載 |
| C2 | critical | `Reader.complete` に `version` が無く鍵を組めない | 成立 | `complete(…, *, sample, version)` に。`RawReply.meta` に `cached`・`key` |
| M1 | major | 鍵同一の要素（付加文のバイト列・スキーマの挿入順・messages の形・末尾改行・`sample` の意味・鍵に入らないもの・選択肢の並び）が未指定 | 成立 | 設計 §4.0 に表を追加。`additionalProperties` を使わないと明記 |
| M2 | major | 再送は空撃ちでは回答だけ。生成・交差検証は再送しない | 成立 | `structured(retry=False)` 既定、回答だけ `retry=True`（I18）。差分表に「変えていない」側で記載 |
| M3 | major | `valid_rate_by_side` が対の形で未定義 | 成立 | 側ごとの定義を §4.3 に。上流 §7.1 に脚注 |
| M4 | major | `Attempt.try` は予約語 | 成立 | 欄名 `index`、JSON は `"try"` |
| M5 | major | 問いの集合を固定して渡す口が無い（J7・S1・S3・S10） | 成立 | `judge(question_set=…)`・`QuestionSet.source` |
| M6 | major | 費用の数え方が未定義。偽読み手の扱い | 成立 | `judge` が `Structured.cached` を役割別に数える。偽読み手は `calibration_calls`。S8 の期待値を明記 |
| M7 | major | `random` 偽読み手が並列で再現しない。偽読み手名の改名 | 成立 | `sha1(seed, messages, sample)`。名前は空撃ちと同じ（`all_undetermined`） |
| M8 | major | 記録に生応答が無い（上流 §8） | 成立 | `Answer.raw`・`Answer.key`。`replay` は raw があれば L2 からも |
| M9 | major | `prompts.py` と `prompts/` の同名衝突・package-data 未記載 | 成立 | `prompt_sets/`・pyproject の package-data・`importlib.resources` |
| M10 | major | 適合検査が `.pair-agent/probes/` の記録に追記しうる | 成立 | `CachedPort(read_only=True, extra=…)`。テストも道具も書かない規則 |
| M11 | major | H12 既定 ON の経路に実データの適合検査が無い | 成立。xc2 のキャッシュ 1,920 行を確認 | 適合検査 ②（base21 ＋ xc2、交差検証あり、`crosscheck.json` と一致） |
| M12 | major | `zero_kind` の同数時と `u1..u4` の分割が未定義 | 成立 | 優先順位 contradiction ＞ invalid ＞ silent（tie は先に決まる）。`u2` ＝ d=0 かつ invalid の軸数 |
| M13 | major | 上流項目 → 設計節の対応表が無く、完了条件を照合できない | 成立 | 付録 A。完了条件を書き換え |
| m1〜m3 | minor | `content` は strip 後／失敗 payload の形／失敗もキャッシュに書くか／`validate` が空撃ちより厳しい | 成立 | m1 明記。m2 は成功だけ書く（I19。空撃ちと違えるので差分表 P2）。m3 は意図的な逸脱として差分表 P3 |
| m4 | minor | 記録の札は κ = 0.5 で付いている | 成立（`probe3.aggregate` が κ を渡していない） | §4.3 に注記（S0a が落ちたときの手がかり） |
| m5・m6 | minor | H7 の違反表記／`同数`・有効票の過半数 | 成立 | §4.1 に反映（表記は上流の規則名に合わせ、差分表 P7） |
| m7 | minor | `Ordinal` の 2 式が浮動小数で割れうる | 成立 | 既定は `level5` の式 1 本。`bounds` は渡されたときだけ。`labels` の長さ検査を F2 に |
| m8 | minor | Δ は値を持つ読み手だけ（空撃ちの要約表と違う） | 成立 | 明記。差分表 P4 |
| m9 | minor | 日本語依存の定数 | 成立 | v1 はコードに置く。§12 に明記 |
| m10・m11 | minor | 単位化の CLI が無い／切片 C は層ごとに | 成立 | `units_chat.py`。切片 C は L2 → L3 → L4 の順に CLI を出す |
| m12 | minor | `types.py` の衝突 | 成立 | `contracts.py` |
| m13 | minor | `@pytest.mark.integration` | 成立 | 併用 |
| m14・m15・m16 | minor | 適合検査の材料ファイル／読み手名の重複／再送の数え方 | 成立 | §9・F2・§4.0 に反映 |
| m17 | minor | 「8,000 回」と「約 6,000 応答」が合わない | 成立 | 実数に置換（out4 15,614 行・空撃ち 2,379 行） |
| m18 | minor | 協議ログに却下の枠が無い | 成立 | この表の形にした |
| 篩 | — | D3（I1〜I17 の確定）は事後確認に変える。D2 と D4 を 1 問に。I9・I10 は確定済み、I16 は値 | 成立（Skill: 原典どおりは問わない・値は問わない） | D は D1・D2 の 2 問に |

却下した指摘: なし（すべて根拠で成立した）。

## 変更ログ

- v3 [2026-09-22T22:20:00+09:00]: 師匠「実装を開始してください」。D1・D2 の読みを AI 判断として記録、実装スプリント 6ddf00eb を起票、status → retrospecting
- v2 [2026-09-22T18:40:00+09:00]: 協議エンジンの評価を反映。**前提崩壊 1**（上流 §5.3 の無効の扱いが測った計器と違った → 上流 v3.5 脚注・設計 §4.3）。完了条件を照合可能な形に書き換え、D を 2 問に絞り、前提の確認表を追加
- v1 [2026-09-22T18:05:00+09:00]: 初版
