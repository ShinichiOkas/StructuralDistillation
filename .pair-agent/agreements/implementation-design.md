---
sprint_id: bb3c8a3c-05a2-49aa-87d9-27e70bc431bd
version: 1
status: deliberating
created_at: 2026-09-22T17:45:00+09:00
updated_at: 2026-09-22T18:05:00+09:00
domain_tags: [implementation-design, python-library, llm-judgment]
slice_size: M（合意ドキュメント＋実装設計文書 `doc/IMPLEMENTATION_DESIGN.md`。協議エンジンの評価＝別コンテキスト。コードは書かない。実装は次のスプリント群）
change_count_premise: 0
change_count_improvement: 0
---

# スプリントゴール

上流設計 v3.4 を「どう組むか」に落とし、**ライブラリを書き始められる状態**にする。
配置・契約（型）・各層の IN → OUT と内部規則・指示の外部化・記録とキャッシュ・実行モデル・失敗の型・テスト・実装の順序が
`doc/IMPLEMENTATION_DESIGN.md` に書かれ、協議エンジンの critical が 0 で、師匠の決定が入っていること。

## 0. 師匠の言葉（原文・2026-09-22）

> 実装設計に進んでください

前スプリントまでの規則（在効。原文は `upstream-design.md` §0・`measurement-2.md` §0）:

> 実装言語はPython。
> 基本的なロジックの設計は他のプロジェクトを参照せずに行うこと。実際の生きたデータを用いて何か実験したいなどの場合において他のプロジェクトを参照してもよい。
> 1.値は全て仮設。動くのは当然。汎用的な値に落とす事が方針 2.僕と同じ感覚にすることは目的ではない。逆に僕の感覚を取り入れるべきではない 3.とくに違和感はないが、僕の違和感は校正ではない。僕に問う意味はない
> すまないがcloudモデルで実行する方針に切り替えてくれ。ollamaのモデルで-cloudがついているもの。これはGPUを使わない

## 過去の仕掛との関係（起草前に確認）

| 仕掛 | 関係 | 扱い |
|---|---|---|
| 前スプリント「測定 2 周目」の振り返り（未実施）と C-3（弱い単一系統に GPU を使うか。未回答） | 師匠が次へ進んだので先送り | このスプリントは GPU を使わない（実接続の検査はクラウドモデル）。振り返りは D4 で伺う。C-3 は答えが来るまで「弱い単一系統は未測定」のまま（上流 §10） |
| 上流設計 §13 の次の測定 5 件（検出項目・同義対・ブループリント・分散成分・羅生門のやり直し） | 実装設計が記録の形を固定すると、後から載せにくくなる | 記録に `schema_version` と `Axis.kind` を最初から置く（設計 I12）。測定そのものは含まない |
| README「まだ決まっていないこと」: パッケージ名・API の形 | このスプリントで決まる | 設計 §2・§4.5（`structural_distillation` / `judge` / `judge_sync` / `replay`）。README は T3 で更新 |
| README「まだ決まっていないこと」: 蒸留の学習基盤 | 決めない | 変更なし |
| `doc/structural_distillation.md` の二重管理（上流 §12 保留） | 触らない | 変更なし |
| ペア固有 Skill `ai-dev-basics`（新規プロジェクトは LLMProviderlib を使う・ストリーミング既定 ON・非同期既定） | 師匠の規則「他のプロジェクトを読まない」と衝突する（LLMProviderlib は師匠の別プロジェクト） | 非同期は従う（I2）。LLMProviderlib は読まずに v1 を組み、接続は `Reader` の口で差し替え可能にする（I1）。**D1 で伺う** |

## タスク

- [x] T1 実装設計 v1 を書く（担当: AI）
- [ ] T2 協議エンジンに合意と設計を評価させ、成立する指摘を反映して v2 にする（担当: AI）
- [ ] T3 師匠の決定 D1〜D4 を受け、設計 v3・README「決まったこと／まだ決まっていないこと」を更新する（担当: 両方）
- [ ] T4 実装スプリント（切片 A）を起票する。completed と次の deliberating を 1 つの手続きで（担当: AI）
- [ ] T5 振り返り（前スプリントの先送り分と合わせるかは D4）（担当: 両方）

## スコープ

文書だけ。**コードは書かない**（テストの雛形も書かない）。
空撃ちの記録（`out4/`）と道具を読んで、適合検査（測定 2 周目のキャッシュだけで `judge()` を再現する）の前提が成り立つかを確かめることは含む。
実装は次のスプリント群（設計 §10 の切片 A〜D）。

## 完了条件

- 上流設計 §4（契約）・§5（層）・§6（H1〜H12）・§7（式と札）・§8（記録）・§4.4（F1〜F7）の各項目が、実装設計のどこかの行に写っている（協議エンジンが照合して漏れ 0）
- 協議エンジンの評価で critical 0（成立した指摘はすべて反映済み）
- 実装判断 I1〜I17 が一覧にあり、師匠が「確定」または修正した
- 師匠の決定 D1〜D4 が本ドキュメントに原文で記録された
- README の「決まったこと／まだ決まっていないこと」が更新された
- 適合検査の前提（鍵の式・キャッシュ行の形・指示の文面が空撃ちと同一にできる）が、空撃ちのコードと `out4/base21/llm_cache.jsonl` を読んで確かめられている

## 前提

- 上流設計 v3.4 は動かさない。実装設計で上流に矛盾が見つかったら「前提崩壊」として数え、上流を直す
- 空撃ちの道具（`probe.py` / `probe3.py` / `probe4_run.py` / `probe4_crosscheck.py`）は動いた実物。ライブラリはその**移植**で、挙動を変えるなら変更履歴に書いて再測定の対象にする
- コアは標準ライブラリだけで書ける（空撃ちが 8,000 回の呼び出しで証明した）
- 手元の Python は 3.13。`requires-python >= 3.11`

## 既知リスク

- 指示のテンプレート化（`$placeholder`）で空撃ちの文面と 1 文字でもずれると鍵が合わず、適合検査が「live 0 件」で落ちる → **適合検査そのものがこれを検出する**。落ちたら指示の差分を直す（設計を変えない）
- 率の定義（設計 I4）が上流 §7.4 の表記（軸ベースの u₂/n）と違う。閾値はこの定義で置かれたので v1 は空撃ちの定義を採る。上流に脚注を足すかは D3 で確認
- 交差検証で外した軸に答えさせない（I6）は、測定 2 周目（答えてから外した）と手順が違う。1 記述 1 呼び出しなので回答行列は同じ、という論証に依っている
- 非同期を本体にすると、同期でしか呼べない利用側（既に走っているイベントループの中）で `judge_sync` が使えない。文書に明記する

## 師匠に決めてもらうこと（D）

- **D1 L0 の接続。** (a) 標準ライブラリだけの Ollama アダプタで始め、他の接続は利用側が `Reader` を実装する（推奨。規則に触れない）／(b) LLMProviderlib を読む許可を出し、アダプタを別ファイルで足す（コアは変えない）／(c) a で始めて b は後で
- **D2 実装の進め方。** (a) 切片 1 つ ＝ スプリント 1 つ（A〜D の 4 本。各層の CLI を師匠が触る人間ゲートを切片の終わりに）（推奨）／(b) 1 スプリント（M〜L）に畳み、層ごとのゲートは残す
- **D3 実装判断 I1〜I17。** 確定でよいか。修正があればどれか
- **D4 前スプリントの振り返り。** 実装設計の合意後にまとめて行う（推奨）／今すぐ／実装が終わってから

## 実行中に採る判断（AI の判断。事後確認対象）

- 協議エンジンの指摘は、根拠資料（上流設計・空撃ちのコードと記録）で**成立するか確かめてから**反映する。成立しないものは理由つきで却下として協議ログに残す
- 協議エンジンが出す「師匠への問い」は 2 つの篩にかける: 師匠の主観・感覚を問うもの（規則: 問う意味はない）と、実装の既定で足りるもの。残ったものだけ D に上げる
- 設計文書の版は、協議エンジン反映で v2、師匠の決定反映で v3

## 協議ログ

- v1 [2026-09-22 18:05] 起草。上流設計 v3.4 と空撃ちの道具を読み直し、設計 v1（§0〜§12・I1〜I17）を書いた。次: 協議エンジンの評価

## 変更ログ

- v1 [2026-09-22T18:05:00+09:00]: 初版
