---
sprint_id: 43898a8f-fb98-4ace-a6d3-dc6b1446d0ba
round: 5
date: 2026-09-23
verdict: 不合格
counts: {critical: 1, major: 4, minor: 9}
---

# 受入検査報告書（スプリント「単一モデル運用」5 回目）

検査者は作った本人ではない。報告書・合意・コミットメッセージの数字は信じず、
`single_model.json`・`c_samples2/single_model.json`・`w5/w5.json`・`records.jsonl` から **自分で数え直した**。
`_note_models` は `tests/conftest.py` の `ScriptedReader` で **38 通りの構成**
（generated / stored / given × 読み手 1 体・2 体同モデル・2 体別モデル・偽読み手混在・偽読み手のみ ×
生成器を省く／渡す × 検証役を省く／1 体／2 体／3 体 × 名前衝突 2 種）を自分で走らせ、4 回目の一覧と突き合わせた。
3 回目の退行そのものの形も**自分で変異体にして**走らせた。
LLM は 1 度も呼んでいない（すべてキャッシュと台本）。一時ファイルは
`C:/Users/shini/AppData/Local/Temp/claude/s--work-develop-StructuralDistillation/4ec11b76-.../scratchpad/` に置いた。
`.pair-agent/probes/` には 1 バイトも書いていない（変異試験で触った `compose.py` は `git checkout` で戻し、
`git status --porcelain` が空であることを確かめた）。

検めた版は `349e7d7`（`docs: 「直した」と書いて直していなかった 2 か所を直す（受入 4 回目 C-1）`）。
4 回目からの差分はこの 1 コミットだけである。

## 判定: **不合格**（critical 1・major 4・minor 9）

critical の一行:

- **C-1**（**今回の直しで入った退行**）: 注意の文面を書き換えたのに `c_samples2/records.jsonl` を作り直さなかったため、
  21 件すべてが**旧版のライブラリ製に戻った**。一方 probe `README.md:50` は
  「3 つの記録（本走行・`w5/`・`c_samples2/`）は**すべて現行版で作り直してある**」と書いたままである。
  ＝ 3 回目 major 7 が閉じた状態が崩れ、同時に**文書が自分の記録について事実でないことを書いている**
  （4 回目 C-1・3 回目 C-2 と同じ形の 3 度目）。

4 回目の指摘そのもの（C-1・M-1・M-2）は**閉じた**。M-3 は 9 件中 8 件が閉じ、m-4 だけ「直した」が言い過ぎ（M-4）。
落ちているのは**今回の直しが持ち込んだ退行 2 件（C-1・M-1）と、同じ形で 3 度目の「記録が無い」（M-2・M-3）**である。

---

## 0. 走らせて確かめたこと（指示された 5 本）

| 検め | 主張 | 実測 | 結果 |
|---|---|---|---|
| `.venv/Scripts/python -m pytest -q` | 207 通る | `207 passed, 1 skipped in 1.85s`（skip は `SD_LIVE` 未設定の実接続） | **主張どおり** |
| `tools/conformance_out4.py` | 21/21・21/21・11/11・外れ 0 | ① 一致 21/21・外れ 0・キャッシュ 1941・**実呼び出し 0** ② 一致 21/21・外れ 0・キャッシュ 2289・実呼び出し 0 ③ 一致 11/11・外れ 0・キャッシュ 2184・実呼び出し 0・外した軸 5（4 題材） | **主張どおり** |
| `tools/mutation_check.py` | 生き残り 0・64 個 | `生き残り 0: []`。`殺した` の行数を数えて **64**。AST でも `MUTANTS` は 64 要素 | **主張どおり** |
| `w5/threshold_sweep.py` | 6 行出る | 6 行。B s1 15/18・B s2 13/18・C s1 7/18・**C s2 7/18**・A s1 16/18・A s2 16/18 が κ=0.667 と κ=0.5 で同じ | **主張どおり**（3 回目 m-1 が閉じた） |
| `single_model_report.py --run . --w5 w5 --c2 c_samples2` との `diff` | 空 | **空**（`DIFF EMPTY`） | **主張どおり**（3 回目 m-7 が閉じた） |

自分で数え直した 6 つの升目（`arm_summary` を使わず、`hit()` の規則と
「NO_EVIDENCE/INSTRUMENT_FAULT の読み手は p に入れない」という報告器の規則を自分で書いて集計）:

```
B s1: 15/18  外した=['m05','m06','m08']
B s2: 13/18  外した=['m04','m05','m08','m14','m18']
A s1: 16/18  外した=['m05','m08']
A s2: 16/18  外した=['m05','m08']        ← 定数 16 ではなく記録から数えた値も 16
C s1:  7/18  外した=['m01','m01b','m04','m05','m08','m10','m14','m16','m18','m19','m20']
C s2:  7/18  外した=['m01','m01b','m03','m04','m05','m07','m10','m14','m16','m18','m19']

C 反転: ['m03','m07','m08','m20']（m03 True→False・m07 True→False・m08 False→True・m20 False→True）
B 反転: ['m04','m06','m14','m18']
A 反転: []（題材まで一致）
m18 標本 2: qwen3.5:4b#a NO_EVIDENCE/有効率 0.333、#b NO_EVIDENCE/有効率 0.167 → 平均 p なし
```

3 回目・4 回目の数え直しと**完全に一致**した。

---

## critical

### C-1 注意の文面を変えたのに `c_samples2/records.jsonl` を作り直さず、probe README は「すべて現行版」と書いたまま

**どこ**

- `.pair-agent/probes/single-model-1/c_samples2/records.jsonl`（**21 件すべて**）
- `.pair-agent/probes/single-model-1/README.md:50`
  「その後も受入のたびにライブラリの注意を直したので、**3 つの記録（本走行・`w5/`・`c_samples2/`）はすべて現行版で作り直してある**。」
- `structural_distillation/compose.py:195-198`（今回書き換えた枝）

**何が問題か**

今回 `_note_models` の `elif len(models) == 1 and not generated:` の文面を書き換えた（3 回目 m-4 への対応）。
この枝が出す注意は **`c_samples2` の走行構成（保存庫から再利用・読み手 2 体同モデル）でまさに出るもの**である。
ところが作り直されたのは `w5/records.jsonl` だけで、`c_samples2/records.jsonl` は 4 回目のまま置かれた。
結果、同じリポジトリの中で **2 つの記録が別々の版の注意を載せている**:

```
w5/records.jsonl（新）      : 読み手は全部同じモデル。この問いの集合を作った生成器は gemma4:31b-cloud・交差検証なし
                              （記録に残った名前）。名前しか残らないので、生成器が読み手と同じモデルかどうかは…
c_samples2/records.jsonl（旧）: この問いの集合を作った生成器は qwen3.5:4b#plan・検証役は qwen3.5:4b#v1・qwen3.5:4b#v2
                              （記録に残った名前）。読み手と同じ系統かどうかはライブラリからは分からない。
                              同じなら、判定は 1 モデルで回っていることになる
```

後者は **現行の `compose.py` がどの経路でも作れない文字列**である（`grep -c "読み手と同じ系統かどうか" structural_distillation/compose.py` → **0**、
`git show 1bc0232:structural_distillation/compose.py` の `:192` と一致）。
つまり 3 回目 major 7（「記録が修正前のライブラリ製」）が**同じ場所で再発**し、
それを打ち消すはずの probe `README.md:50` の文が**事実でなくなった**。
しかも「同じなら、判定は 1 モデルで回っていることになる」は、3 回目 m-4 が「無意味」として消させた当の一文である。
リポジトリに残る記録が、撤回済みの言い方を 21 件抱えている。

**どう確かめたか**

```
$ grep -rln "読み手と同じ系統かどうか" . | grep -v "^./.git/"
./.pair-agent/acceptance/43898a8f-single-model-3.md          （3 回目の報告書が引用している分）
./.pair-agent/probes/single-model-1/c_samples2/records.jsonl

$ grep -c "読み手と同じ系統かどうか" .pair-agent/probes/single-model-1/c_samples2/records.jsonl
21
$ wc -l < .pair-agent/probes/single-model-1/c_samples2/records.jsonl
21

$ grep -c "読み手と同じ系統かどうか" structural_distillation/compose.py
0

# 3 つの記録に載っている注意の集合（自分で全件走査）
本走行     : 自己交差検証 / 読み手が全部同じモデル / 生成器も読み手も同じモデル   … すべて現行の文面と一致
w5/       : 読み手が全部同じモデル / 読み手は全部同じモデル。…名前しか残らないので… … 現行版
c_samples2: 読み手が全部同じモデル / この問いの集合を作った生成器は…同じなら、判定は 1 モデルで… … 旧版

# 現行ライブラリが同じ構成で何を出すか（台本の読み手・LLM 呼び出し 0）
$ .venv/Scripts/python .../scratchpad/notes_matrix.py
  S 再利用・読2(同)・別系統の生成器名   ['読同一', '名前告知']
    → 名前告知の本文は「読み手は全部同じモデル。…名前しか残らないので…」（新しい文面）
```

差分の裏づけ:

```
$ git show --stat 349e7d7 | grep records
 .pair-agent/probes/single-model-1/w5/records.jsonl |  84 +-      ← w5 だけ
（c_samples2/records.jsonl は 349e7d7 に現れない。記録の全欄を突き合わせても「違う欄: {}」）
```

**どう直せば合格にできるか**（提案）

- `c_samples2/records.jsonl` をキャッシュだけで作り直す（実呼び出し 0 で作れることは本走行・w5 で実証済み）。
  作り直したら、測定値（`readings`・`summary`・`question_set`・`input`）が 21/21 で不変であることを併記する
- 併せて「注意の文面を変えたら 3 つの記録を作り直す」を手順として書き残す（今回はそれが無いから 1 つ落ちた）

---

## major

### M-1 `w5/w5.json` を上書きして、実測の時計（38.5 分）と実呼び出し数（1,008）を消した。どこにも記録が無い

**どこ**: `.pair-agent/probes/single-model-1/w5/w5.json`（21 題材すべて）、
`.pair-agent/probes/single-model-1/README.md:40`・`:53`、`.pair-agent/agreements/single-model-operation.md:64`

**何が問題か**

3 回目 m-2（「`A2 = 16` を定数で埋めている」）を閉じるために `w5_run.py` を直し、**W5 を丸ごと引き直した**。
引き直しはキャッシュだけ（実呼び出し 0）なので測定値は不変だが、`w5.json` は**結果ファイルごと上書き**されたので、
走行時の実測が消えた:

| 欄 | 4 回目（`1bc0232`） | いま（`349e7d7`） |
|---|---|---|
| `seconds` の合計 | **38.5 分** | **0.0 分**（題材あたり 0.1 秒） |
| `cost_s1`+`cost_s2` の `live` 合計 | **1,008** | **0**（`cached` 1,512） |

ところが 3 文書は実測としてこう書いている:

- probe `README.md:40`「ローカル **1,008 呼び出し**（21 題材 × 6 軸 × 2 側 × 標本 2 × 読み手 2 体）・**約 38 分**」
- 合意 `:64`「腕 W5 はこれに加えてローカル **1,008 呼び出し・約 38 分**」
- ジャーナル 2026-09-23T14:55 の経過「標本 2・1008 呼び出し・約 38 分」

**この 3 つを裏づける数字は、作業ツリーのどこにも無くなった**（git の履歴にしか残っていない。
私は `git show 1bc0232:.../w5.json` で 38.5 分・1,008 を再現して、主張自体は正しいと確かめた）。
さらに probe `README.md:53` は「⚠ `single_model.json` は走行時のまま（**`seconds` が実測なので上書きしない**）」と
方針を掲げている。同じ理由が当てはまる `w5.json` にだけ、その方針が適用されなかった。
上書きしたこと自体が**どの文書にも 1 行も書かれていない**（`grep -rn "w5.json" --include=*.md .` は acceptance を除くと 0 件）。

**どう確かめたか**

```
$ .venv/Scripts/python .../scratchpad/secs.py
8fdc1a4 seconds 合計 38.5 分  live合計 1008
089410b seconds 合計 38.5 分  live合計 1008
e2fadba seconds 合計 38.5 分  live合計 1008
1bc0232 seconds 合計 38.5 分  live合計 1008
HEAD    seconds 合計  0.0 分  live合計 0   （cached 1512・strong も cached 1512／live 0）

$ .venv/Scripts/python -c "...w5.json の先頭 3 件..."
m01  0.1 {'live': 0, 'cached': 24, ...}
m01b 0.1 {'live': 0, 'cached': 24, ...}
m02  0.1 {'live': 0, 'cached': 24, ...}
```

⚠ **測定値そのものは壊れていない**。旧版と全欄を突き合わせた結果:

```
題材数 21 -> 21・鍵一致 True
新たに増えた欄: {'strong_readers_s2': 21, 'strong_delta_s2': 21, 'strong_cost_s2': 21}
変わった欄:     {'cost_s2': 21（live 48 → cached 48）, 'notes': 21, 'seconds': 21}
→ weak_readers_s1 / weak_readers_s2 / strong_readers_s1 / delta_s1 / delta_s2 / strong_delta_s1 は 21/21 で不変
```

**標本 1 の値は 1 つも動いていない**（依頼文の確認事項。`git show` との突き合わせで確認）。

**どう直せば合格にできるか**（提案）: `seconds` と `cost_s*.live` を走行時の値に戻すか（git から引ける）、
`w5.json` も引き直しで上書きしたこと・実測の時計は `1bc0232` にしか無いことを probe README に 1 行書く。

### M-2 4 回目の minor（m-12〜m-17）の対応／見送りが、合意にもジャーナルにも 1 行も無い

**どこ**: `.pair-agent/agreements/single-model-operation.md:183-193`（4 回目の対応表）

**何が問題か**

4 回目の対応表は **C-1・M-1・M-2・M-3 の 4 行だけ**で、minor の行が 1 つも無い。
4 回目の報告書は `counts: {critical: 1, major: 3, minor: 12}` で、そのうち
**m-12〜m-17 の 6 件は 4 回目が新しく出した指摘**（「そのほか」の節）であり、末尾の「再受入で見るところ」の
5・6・7 番に明記されている。現物を当たると:

| 4 回目 | 現状 | 判定 |
|---|---|---|
| m-12 名前とモデル名の取り違え・同値な 2 条件 | `compose.py:188-189` そのまま | **手つかず** |
| m-13 `compose.py:155` のドックストリング 15/18 | 「13〜15/18 だった（標本数で動く）」に直った | **閉じた** |
| m-14 上流 `:525` の無条件 15/18 | `:532`「13〜15/18 を当てた。標本数で動く」に直った | **閉じた** |
| m-15 「題材が7/18」の空白落ち | `compose.py:192-193` そのまま | **手つかず** |
| m-16 上流 `:415` の p の中央値の定義 | `:415`「題材ごとの平均で 0.833・読み手ごとで 0.817」に直った | **閉じた** |
| m-17 変異体 2 個が名前どおりの穴を突いていない | `tools/mutation_check.py:60-65` そのまま | **手つかず** |

つまり **6 件中 3 件は直り、3 件は手つかず**なのに、**どちらについても記録が無い**。
これは 3 回目 M-8 →4 回目 M-3 と**同じ指摘の 3 度目**である。
4 回目 M-3 の行（`:193`）自身が「⚠ 見送りは 1 件もない」と書いているのに、
その同じ表に 4 回目の minor の行を作っていない。

**どう確かめたか**

```
$ sed -n '183,200p' .pair-agent/agreements/single-model-operation.md
（C-1・M-1・M-2・M-3 の 4 行で表が終わり、次は「## 完了条件」）

$ grep -n "m-12\|m-13\|m-14\|m-15\|m-16\|m-17" .pair-agent/agreements/single-model-operation.md
（0 件）

$ sed -n '55,70p' tools/mutation_check.py
    ("judge 既定の生成器を注意の対象にしない", "compose.py",
     "    resolved_planner = planner if planner is not None else (real[0] if real else None)",
     "    resolved_planner = planner"),                                    ← 4 回目 m-17 のまま
    ("judge 既定の検証役を注意の対象にしない", "compose.py",
     "    resolved_verifiers = list(verifiers) if verifiers is not None else real",
     "    resolved_verifiers = list(verifiers) if verifiers is not None else []"),   ← 同

$ sed -n '188,193p' structural_distillation/compose.py
    same = ((planner is not None and model_of(planner) in models) if generated else
            (qs.planner in models and any(model_of(r) == qs.planner for r in real)))   ← 4 回目 m-12 のまま
                     "7/18 まで落ちた（…）"        ← 「題材が7/18」の空白落ち（m-15）のまま
```

### M-3 受入 4 回目とその対応が、スプリントのジャーナルに 1 件も無い

**どこ**: `.pair-agent/sprints/43898a8f-fb98-4ace-a6d3-dc6b1446d0ba.json`

**何が問題か**

`経過` は 10 件で、**最後は `2026-09-23T19:30`（受入 3 回目の不合格）**。
4 回目の不合格（critical 1）も、今回の直しも、1 件も入っていない。
`349e7d7` はジャーナルを 1 文字も触っていない（`git show --stat` に現れない）。

2 回目は `e2fadba`（`sprint: 受入 2 回目の不合格と対応をジャーナルに記録`）、
3 回目は `1bc0232` で、それぞれジャーナルに書かれている。**今回だけ途切れた**。
プロジェクトの `CLAUDE.md` は「スプリントの経過（**正**）: `.pair-agent/sprints/<sprint_id>.json`」
「経過（`経過` 配列）は最初から `.pair-agent/sprints/<sprint_id>.json` に書く」と定めており、
4 回目 M-3 でも「ジャーナルの経過にも『minor』の語が無い」と指摘されている。

**どう確かめたか**

```
$ .venv/Scripts/python -c "...経過を全件読む..."
status: executing 経過件数: 10
最後の経過: 2026-09-23T19:30:00+09:00     （受入 3 回目の不合格・「腕 C も標本 1→2 で 4 題材…」）

$ git log --oneline -3 -- .pair-agent/sprints/43898a8f-fb98-4ace-a6d3-dc6b1446d0ba.json
1bc0232 fix: 既定の生成器でも注意を出す（受入 3 回目 C-1 の退行）＋数え直し
e2fadba sprint: 受入 2 回目の不合格と対応をジャーナルに記録（W5・標本 2・注意の穴）
6344c91 fix: 1 モデル運用の注意を全経路で出す（受入 C4・M12・M13）
```

### M-4 合意 `:193` は m-4（機械的な問いへの偽陽性）を「直した」と書くが、偽陽性は今も出る。しかも新しい傷が入った

**どこ**: `.pair-agent/agreements/single-model-operation.md:193`、`structural_distillation/compose.py:195-198`、
`.pair-agent/probes/single-model-1/single_model_run.py:105-108`（`planner="mechanical"` の経路）

**何が問題か**

3 回目 m-4 は「検出項目の問いの集合（`planner="mechanical"`・LLM が作っていない）に対しても**無意味な注意が出る**」だった。
今回の直しは**文面を書き換えただけ**で、**出る／出ないの条件（`elif len(models) == 1 and not generated:`）は 1 文字も変えていない**。
`planner="mechanical"` の問いでは今も注意が出る:

```
=== 検出項目（planner=mechanical・交差検証なし・読み手 2 体同モデル）の注意 ===
 - 読み手が全部同じモデル（qwen3.5:4b）。読み手間の差 Δ は同じモデルの揺れで、読み手非依存（Q1）の計器にはならない
 - 読み手は全部同じモデル。この問いの集合を作った生成器は mechanical・交差検証なし（記録に残った名前）。
   名前しか残らないので、生成器が読み手と同じモデルかどうかはライブラリからは分からない
```

機械的に組んだ軸には**生成器のモデルというものが存在しない**のに、いまも「生成器が読み手と同じモデルかどうかは分からない」と言う。
確かに「同じなら、判定は 1 モデルで回っていることになる」という最も無意味な一文は消えた（**改善はある**）。
だが合意 `:193` の「**すべて直した**」「⚠ 見送りは 1 件もない」は言い過ぎである。

加えて、頭に足した「**読み手は全部同じモデル。**」が新しい傷を作った:

| 構成 | 出る注意 | 問題 |
|---|---|---|
| 読み手 **1 体**・問いを渡す | `読み手が 1 体（qwen3.5:4b#a）…` ＋ `読み手は全部同じモデル。…` | 1 体しかいないのに「全部同じモデル」と言う。直前の行と噛み合わない |
| 読み手 2 体同モデル・問いを渡す | `読み手が全部同じモデル（qwen3.5:4b）…` ＋ `読み手は全部同じモデル。…` | **直前の注意と同じことを 2 度言う** |

しかもこの 2 つ目の形は、`c_samples2` の実走行構成そのものである（C-1 で作り直せば、記録にこの重複がそのまま載る）。

**どう確かめたか**: `.../scratchpad/mech.py`（`planner="mechanical"`・`kind="detection"` の `QuestionSet` を
`question_set=` で渡し、台本の読み手 2 体／1 体で `judge()` を走らせる。LLM 呼び出し 0）。
上の出力はそのまま貼ったもの。`.../scratchpad/notes_matrix.py` の
`S 再利用・読1(実)` → `['読1体', '名前告知']`、`D 渡す・読2(同)・別系統` → `['読同一', '名前告知']` でも同じ重複を確認。

---

## minor

- **m-1**（4 回目 m-12 の再掲・手つかず）: `compose.py:188-189`
  `(qs.planner in models and any(model_of(r) == qs.planner for r in real))` は
  **2 つの条件が論理的に同値**（`models` は `{model_of(r) for r in real}`）で後半が死んでおり、
  記録の生成器「名」が読み手の「モデル」名と一致すると実体が別モデルでも言い切る。
  私の総当たりでも `D／S 名前衝突（生成器名＝読み手のモデル名・実体別）` → `['読同一', '生読同一']` で誤断定が再現した。
- **m-2**（4 回目 m-15・3 回目 M-6 の再掲・手つかず）: `compose.py:192-193` の文字列連結で空白が落ち、
  実際の注意は「想定に合った題材が7/18 まで落ちた」と出る（本走行の記録 21 件にもこの形で載っている）。
- **m-3**（4 回目 m-17 の再掲・手つかず）: `tools/mutation_check.py:60-65` の 2 変異体は、名前は
  「既定の生成器／検証役を**注意の対象にしない**」だが、中身は生成そのものを `InputError` にする／交差検証そのものを消す。
  ⚠ 実害は無い（下記のとおり、退行そのものの形は新しいテストがきちんと殺す）。**名前が担保している内容とずれている**という指摘。
- **m-4**（4 回目 M-2 の 3 点目が残る）: 1 モデル構成では `compose.py:178` が `elif` なので
  **自己交差検証の注意が出て拒否権は出ない**。拒否権は 1 モデルのときにも成り立つ事実である。
  今回足した上流 `:444-445` は「1 モデル運用とは別に…も出す」と書くが、「1 モデルのときは出ない」には触れていない。
- **m-5**: 合意 `:64`「腕 C の標本 2 はローカル **約 500 呼び出し**」対 probe `README.md:41`「**488 呼び出し**」。
  同じ量が 2 通り。`report.md:139` は「回答 488」で probe README 側と揃う。
- **m-6**: `doc/UPSTREAM_DESIGN.md` の `## 変更履歴` に、3〜5 回目で §10 を書き直したことの行が無い
  （最新は `v3.8`）。その `v3.8`（`:551`）は「強い軸 × 弱い読み手 **15/18**」と無条件のままで、
  本文 `:418` の「15/18 ／ 13/18」と読み比べると標本の条件が落ちて見える。
- **m-7**: 合意 `:89`「うち **3 件**は判定の境目（平均 p が **0.3 / 0.4 / 0.6 / 0.7** のすぐ近く）」は、
  3 件に対して 4 つの値を並べている。私の数え直しでは m04 が 0.3 の際・m06 が 0.4 の際・m14 が 0.7 の際で、境目は 3 つ。
- **m-8**: 合意の frontmatter が `version: 3`・`updated_at: 2026-09-23T15:05:00+09:00` のまま。
  3 回目・4 回目の対応表を追記した後も動いていない（`## 変更ログ` にも行が無い）。
- **m-9**: probe `README.md:50-52` は「3 つの記録」の作り直しを 1 か所で保証しているが、
  実際に何を根拠に「現行版」と言えるかの確かめ方（引き直しコマンド）が `## 再現` に無い。
  `single_model_report.py` と `threshold_sweep.py` と `w5_run.py` は載ったが、記録の作り直しだけ手順が無い。

---

## 4 回目の指摘の開閉（1 件ずつ）

| # | 4 回目の指摘 | 開閉 | 根拠 |
|---|---|---|---|
| **C-1** | 3 回目 major 1・2 が 合意 `:87-88` と 上流 `:419` に残るのに「直した」と書いた | **閉じた** | 合意 `:87-90` が「**数**が動くのは腕 B だけ…ただし **C は中身が 4 題材入れ替わっている**（m03・m07 が外れ、m08・m20 が当たる。7/18 が同じなのは相殺の偶然）」「**m18 は境目ではない**: 両方の読み手が『本文に根拠が無い』（有効率 0.33・0.17）」に。上流 `:420-421` も「弱い軸の腕も中身は 4 題材入れ替わる（数が同じなのは相殺）」「1 件は両読み手の有効率が崩れて値が出なくなったもの」に。`report.md:103-104` と**同じ内容**であることを 3 文書を並べて確認。私の数え直し（C 反転 m03・m07・m08・m20／m18 有効率 0.333・0.167）とも一致 |
| **M-1** | 上流 `:416` の「軸の出所と読み手**だけ**」 | **閉じた** | `:417` が「軸の出所と読み手を入れ替えた 2×2」に。さらに `:423-424` に「⚠ この 2×2 は…閾値（κ 0.667 対 0.5）・交差検証の有無・偽読み手の同居が揃っていない」と確かめ方が入った。`grep -rn "軸の出所と読み手" --include=*.md .` で「だけ」は 1 件も残っていない（合意 `:159`・`:191` は過去の対応表の引用） |
| **M-2** | 拒否権の注意が既定の多モデル呼び出しで出るのに未文書化 | **閉じた** | 注意の文面に「測定では、生成器を検証役に入れても外した軸の数は同等だった。⚠ これは 1 モデル運用とは別の注意で、別々のモデルでも出る」が入った（`compose.py:179-181`）。`README.md:166-167` に「⚠ 検証役が 2 体でその 1 体が生成器のときは…**生成器が自分の軸に拒否権を持ちます**。モデルが別々でも起きるので、判定に注意が付きます」。上流 `:444-445` に「⚠ 1 モデル運用とは別に…これらはモデルが別々でも出る」。README `:165` の「検出数は同等」と並べて読める。文面の根拠（`1/126`・`6/66`）は上流 `:243` にあり、`l1.py:174` の `opposing * 2 > len(verifiers)` とも整合。⚠ 3 点目（1 モデルのときだけ黙る）は残る（minor m-4） |
| **M-3** | 3 回目の minor 11 件の対応／見送りの記録が無い | **ほぼ閉じた**（9 件中 8 件） | 合意 `:193` に 9 件を列挙した行が入り、m-5・m-6 は 3 回目で対応済みと明記。現物は下表のとおり **m-4 以外の 8 件が実際に閉じた**。m-4 だけ「直した」が言い過ぎ（**major M-4**） |

### 3 回目の minor 11 件の現物（M-3 の裏取り）

| 3 回目 | 現状 | 判定 |
|---|---|---|
| **m-1** `threshold_sweep.py` に C の標本 2 の行が無い | 6 行出る。C s2 も κ=0.667／0.5 の両方で 7/18。私も走らせて再現 | **閉じた** |
| **m-2** `A2 = 16` を定数で埋めている | `A2s = arm_summary(w5rows, "strong_readers_s2", exp5)` に。`w5_run.py:110-117` が標本 1・2 を回す。私の独立集計でも A s2 ＝ 16/18（外したのは m05・m08 で標本 1 と同じ題材） | **閉じた**（⚠ 引き直しで `w5.json` の実測を消した＝ **major M-1**） |
| **m-3** 読み手の寄与の符号が逆向き | `report.md:92`「読み手の寄与（**A − B**・軸は同じ）: 想定一致 **+1** 件」。`:98` の表も A−B。生成器は `single_model_report.py:322-323` | **閉じた** |
| **m-4** `planner="mechanical"` の問いに無意味な注意 | 文面は変わったが**今も出る**。条件は 1 文字も変わっていない | **部分**（**major M-4**） |
| **m-5** 偽読み手だけの構成で注意 0 件 | 3 回目で対応済み。私の総当たりでも `偽のみ` が出る | **閉じた** |
| **m-6** 名前とモデル名を混ぜて誤断定 | 3 回目で半分。残りは 4 回目 m-12 ＝ いまも手つかず | **部分**（minor m-1） |
| **m-7** 引き直しと `report.md` が末尾の空行 1 行だけ違う | `print(text, end="")`（`:393`）。`diff` は**完全に空** | **閉じた** |
| **m-8** 合意 `:148` の「pytest 205」 | 合意 `:152` が「pytest **204**」。2 回目の報告書 `:31` が記録した数と一致する（3 回目 `:38` は 205） | **閉じた** |
| **m-9** 合意 `:95` の評価語「かなり使える」 | 「**弱い 4B を読み手にした腕は 15/18（標本 1）・13/18（標本 2）**」に。`grep -rn "かなり使える" --include=*.md .` は 0 件 | **閉じた** |
| **m-10** 「交差検証の有無も効かない」は A・B 側だけの確認 | 合意 `:95-97`「交差検証の有無は **A・B の側でだけ**確かめた…⚠ **腕 C を交差検証 OFF に揃えた場合は測れていない**（自己交差検証が外した軸の回答が無いため。受入 3 回目 m-10）」 | **閉じた** |
| **m-11** `c_samples2/run.log` の先頭が `[m01] 済み` | probe `README.md:42-43` に理由と「`seconds` の合計（19.0 分）には m01 の時計が別の呼び出しのものとして入っている」。私も合計を数えて **19.0 分**・`run.log` の先頭が `[m01] 済み` を確認 | **閉じた** |

---

## 特に検めてほしかった 4 点（依頼文）への回答

### ① 5 文書で同じ事実の書き方が食い違っていないか（4 回目に直した 2 か所が全部の文書で直っているか）

| 事実 | 合意 | `report.md` | 上流 | README | probe README | 判定 |
|---|---|---|---|---|---|---|
| **腕 C も 4 題材入れ替わる** | `:88` ○ | `:103` ○ | `:420` ○ | — | `:36`（数字のみ・誤りなし） | **揃った**（4 回目 C-1 は閉じた） |
| **m18 は境目でなく有効率の崩れ** | `:90` ○ | `:104` ○ | `:421` ○ | — | — | **揃った**（同上） |
| 「軸の出所と読み手**だけ**」 | — | `:77` ○ | `:417` ○ | — | — | **揃った**（M-1 は閉じた） |
| **13〜15/18**（標本で動く） | `:79`・`:99` ○ | `:88`・`:100`（標本 1 の表なので 15/18）○ | `:418` ○ / `:532` ○ | `:161` ○ | `:35` ○ | **揃った**（4 回目 m-14 も閉じた）。⚠ 変更履歴 `:551` だけ無条件（minor m-6） |
| `compose.py` のドックストリング | — | — | — | — | — | `:155` が「13〜15/18 だった（標本数で動く）」○（4 回目 m-13 は閉じた） |
| **p の中央値の定義** | — | `:45` ○ | `:415` ○（題材ごと 0.833・読み手ごと 0.817） | — | — | **揃った**（2 回目 m-2／4 回目 m-16 は閉じた） |
| **非排他 5.6 倍** | `:119`・`:142`・`:160` ○ | `:68` ○（6.6 は注記つき） | `:437`・`:524` ○ | — | — | **揃っている** |
| **拒否権** | — | — | `:444` ○ | `:166-167` ○ | — | **揃った**（M-2） |
| **記録は現行版で作り直し** | — | — | — | — | **`:50` ✗** | **C-1**（`c_samples2` は旧版） |
| **W5 は 1,008 呼び出し・約 38 分** | `:64`（約 500 と混在） | `:142`（1008・キャッシュの行数から） | — | — | `:40`（488 と整合） | **M-1**（`w5.json` に裏づけが無い）・**minor m-5** |
| `pytest` の数 | `:152`「204」○ | — | — | — | — | **閉じた**（4 回目 m-8） |

### ② `_note_models` の書き換えで、前に出ていた注意が消えていないか（総当たり）

`ScriptedReader` で 38 構成を自分で走らせ（LLM 呼び出し 0）、4 回目の報告書の一覧と 1 行ずつ突き合わせた。
略号 ＝ XC無・自己XC・拒否権・読1体・読同一・生読同一・名前告知・偽のみ。

| 構成 | 4 回目 | いま | 判定 |
|---|---|---|---|
| **G** 読2(同)・生省略・検省略 | 自己XC・読同一・生読同一 | 同じ | **消えていない** |
| G 読2(同)・生渡す・検省略／生省略・検2渡す／生渡す・検2渡す | 同上 | 同じ | 消えていない |
| G 読2(同)・生渡す・検1渡す | XC無・読同一・生読同一 | 同じ | 消えていない |
| G 読1(実)・省略/省略 | XC無・読1体・生読同一 | 同じ | 消えていない |
| G 読1(実)・生渡す・検2渡す | 自己XC・読1体・生読同一 | 同じ | 消えていない |
| G 読2(**異**)・省略/省略 | 拒否権 | 拒否権 | 消えていない |
| G 読2(異)・生渡す(第3のモデル)・検省略 | （なし） | （なし） | 増えていない |
| G 読2(同)+偽1／+偽2／偽が先頭 | 自己XC・読同一・生読同一 | 同じ | 消えていない |
| G 読1(実)+偽1 | XC無・読1体・生読同一 | 同じ | 消えていない |
| G 偽のみ | `InputError: 生成器が要る` | 同じ | 変わらず |
| G 生成器が検証役2の1体を兼ねる（読み手別モデル） | 拒否権 | 拒否権 | 消えていない |
| G 生成器が検証役**3**の1体を兼ねる | （なし） | （なし） | 正しい（`l1.py:174` と照合。3 体なら 2 票で外れる） |
| G 読2(同)・検証役だけ別モデル2体 | 読同一・生読同一 | 同じ | 消えていない |
| G 交差検証OFF(budget) | 読同一・生読同一 | 同じ | 消えていない |
| **S** 再利用・読2(同)・別系統の生成器名 | 読同一・名前告知（＋保存差） | 読同一・名前告知 | 消えていない（保存差は `_note_differences` の別経路） |
| S 再利用・読1(実) | 読1体・名前告知 | 同じ | 消えていない（⚠ 文面が「読み手は全部同じモデル」に。**M-4**） |
| S 再利用・読2(異) | （名前告知なし） | （なし） | 変わらず |
| S 再利用・読2(同)+偽1／検2渡す | 読同一・名前告知 | 同じ | 消えていない |
| S 再利用・偽のみ／偽1体 | 偽のみ | 偽のみ | 消えていない |
| **D** 渡す・読2(同)・別系統／読1(実)／+偽2 | 読同一 or 読1体 ＋ 名前告知 | 同じ | 消えていない |
| D 渡す・読2(異) | （なし） | （なし） | 変わらず |
| D 渡す・偽のみ2体／偽1体 | 偽のみ | 偽のみ | 消えていない |
| D 渡す・交差検証なしの問い・読2(同)／読1 | XC無 ＋ 読同一/読1体 ＋ 名前告知 | 同じ | 消えていない |
| D 名前衝突（記録の生成器名＝読み手の**名前**・実体別） | 読同一・名前告知 | 同じ | 正しく言い切らない |
| D／S 名前衝突（記録の生成器名＝読み手の**モデル**名・実体別） | 読同一・**生読同一**（誤断定） | 同じ | **誤断定が残る**（minor m-1） |

**結論: 出る／出ないの組み合わせは 4 回目と 1 つも変わっていない。消えた注意は無い。**
変わったのは `elif len(models) == 1 and not generated:` の**文面だけ**で、条件は同一である。
その文面に足した「読み手は全部同じモデル。」が重複と 1 体時の食い違いを生んだ（**M-4**）ことと、
それを載せ直していない記録がある（**C-1**）ことが今回の傷である。

**退行の検出力そのものは落ちていない**。3 回目の退行の形（注意に生の引数を渡す）を私が変異体にして走らせた:

```
$ .venv/Scripts/python .../scratchpad/mut_r3.py
R3 生の planner と verifiers を注意に渡す: 殺した
R3a 生の planner だけ:                     殺した
R3b 生の verifiers だけ:                   殺した
復元 ok: True   （その後 git checkout で完全復元・git status --porcelain は空）
```

### ③ `w5/w5_run.py` の書き換えで標本 1 の値が変わっていないか（`git show` で突き合わせ）

**変わっていない。**`git show 1bc0232:.../w5.json` と HEAD を全欄で突き合わせた:

```
題材数 21 -> 21・鍵一致 True
新たに増えた欄: {'strong_readers_s2': 21, 'strong_delta_s2': 21, 'strong_cost_s2': 21}
変わった欄:     {'cost_s2': 21, 'notes': 21, 'seconds': 21}
→ weak_readers_s1・weak_readers_s2・strong_readers_s1・delta_s1・delta_s2・strong_delta_s1 は 1 件も動いていない
```

3 つの `records.jsonl` も全件を構造で比較した:

| 記録 | 件数 | 違う欄 | `matrices[].answers` の差 | `cost.live` |
|---|---|---|---|---|
| `records.jsonl`（本走行） | 21 → 21 | **なし** | **なし** | 0 |
| `c_samples2/records.jsonl` | 21 → 21 | **なし** | **なし** | 0 |
| `w5/records.jsonl` | 42 → 42 | `at`・`notes` のみ | **なし** | 0 |

**測定値はどこも動いていない。実呼び出しも 0。**
`single_model.json`（本走行・`c_samples2`）は `git show --stat 349e7d7` に現れず、**上書きされていない**（主張どおり）。
⚠ 上書きされたのは `w5/w5.json` で、その `seconds`・`live` が消えた（**M-1**）。作業ツリーは `git status --porcelain` が空。

### ④ 合意の完了条件（`## 完了条件` `:197-200`）への当てはめ

| 完了条件 | 判定 | 根拠 |
|---|---|---|
| 4 腕の記録がリポジトリに取り込まれ、判断規則に照らした読みが合意に書かれている | **ほぼ満たす**（傷あり） | 記録・走行器・読み取り器がそろい、`report.md` は引き直しと**完全一致**。合意 `:87-90` の読みは私の数え直しと一致し、4 回目 C-1 が閉じた。⚠ `c_samples2/records.jsonl` が旧版製に戻り（**C-1**）、`w5.json` の実測が消えた（**M-1**） |
| 上流設計に「1 モデルのとき何を保証し、何を保証しないか」が書かれている（§10 の適用限界） | **満たす** | `:415-424` に 2×2 と揃っていない 3 つ、`:420-421` に腕 C の入れ替えと m18、`:441-445` に注意の枠（1 モデル運用のもの／それとは別のもの）、`:532` に 13〜15/18。4 回目の C-1・M-1・M-2・m-14・m-16 がすべて §10 に届いた |
| 実装で直すべきものが出たら、直すか、次の題として書く | **満たさない** | `compose.py:188-189` の名前／モデル名の取り違えと死んだ条件（4 回目 m-12）、`:192-193` の空白落ち（m-15）、`mutation_check.py:60-65` の名前と中身のずれ（m-17）は、**直してもいないし次の題として書いてもいない**（**M-2**） |
| 受入で critical 0 | **満たさない** | critical 1（**C-1**） |

---

## 確かめた結果、主張どおりだったもの（沈黙で済ませない）

| 主張 | 自分で確かめた値 | 判定 |
|---|---|---|
| pytest 207 通る | `207 passed, 1 skipped` | **主張どおり** |
| 適合検査 21/21・21/21・11/11・外れ 0 | すべて一致・実呼び出し 0・外した軸 5（4 題材） | **主張どおり** |
| 変異体 64 個・生き残り 0 | `殺した` 64 行・`生き残り 0: []`。AST でも `MUTANTS` は 64 要素 | **主張どおり** |
| `threshold_sweep.py` が 6 行出る | 6 行。C の標本 2 も両閾値で 7/18 | **主張どおり**（3 回目 m-1 が閉じた） |
| 引き直しと `report.md` が完全一致 | `diff` が空（末尾の空行も含めて） | **主張どおり**（3 回目 m-7 が閉じた） |
| A の標本 2 を定数で埋めず数える | `arm_summary(..., "strong_readers_s2", ...)`。私の独立集計でも 16/18（外したのは m05・m08） | **主張どおり** |
| 2×2 の 6 升（16/15/7 と 16/13/7）・寄与 +8/+1 と +6/+3 | 自分で集計して全一致。R-g の読みも 標本 1「生成器」・標本 2「両方」 | **主張どおり** |
| 腕 C の 4 題材入れ替え（m03・m07 が外れ、m08・m20 が当たる。相殺で 7/18） | 完全一致。m03・m20 は標本 2 で平均 p が無くなる | **主張どおり**（今回は合意・上流にも届いた） |
| m18 は標本 2 で有効率 0.333／0.167 の崩れ | `w5.json` から再計算して一致 | **主張どおり** |
| 拒否権の注意の中身 | `l1.py:174` の `opposing * 2 > len(verifiers)` と照合。検証役 2 体なら 2 票必要・3 体なら 2 票で外れる。コードも `len == 2` に限る。私の総当たりでも 3 体では言わない | **支持されている** |
| 「生成器を検証役に入れても外した軸の数は同等」 | 上流 `:243` の `1/126`・`6/66`（基準は `0/126`・`5/66`）と整合。README `:165` とも並ぶ | **支持されている** |
| 3 つの記録の測定値が不変・実呼び出し 0 | `readings`・`summary`・`question_set`・`input` が 21/21・21/21・42/42 で全件一致。`cost.live` はすべて 0 | **主張どおり** |
| `single_model.json`（本走行・`c_samples2`）を上書きしていない | `git show --stat 349e7d7` に現れない。作業ツリーも clean | **主張どおり** |
| 標本 1 の値が変わっていない | `git show 1bc0232` と全欄突き合わせ。`weak_readers_s1`・`strong_readers_s1`・`delta_s1`・`strong_delta_s1` は 1 件も動かず | **主張どおり** |
| 3 回目の退行そのものの形を、いまのテストが殺す | 3 通りの変異体すべて「殺した」 | **壊れていない** |
| `CLAUDE.md` と `GEMINI.md` の同期 | `diff` が空 | **主張どおり** |
| 1〜4 回目で確認済みの数字（R-a 7/18・R-d 適合率 1.00/再現率 0.27・p≥0.8 で想定が支持でない 6/18・他系統を借りても 7/18・弱率 0.944/0.849/0.698・非排他 5.6 倍・札が 7/21 で割れ p 同じ 4 件） | `report.md` を引き直して全一致 | **壊れていない** |

---

## 再受入で見るところ

1. **C-1**: `c_samples2/records.jsonl` を現行版で作り直したか（実呼び出し 0・測定値 21/21 不変を併記）。
   probe `README.md:50` の「すべて現行版」が、作り直した後に書かれているか
2. **M-1**: `w5/w5.json` の実測（`seconds` 38.5 分・`live` 1,008）を戻したか、
   または上書きしたことと実測の在りかを probe README に書いたか
3. **M-2**: 4 回目の minor（m-12〜m-17）を 1 件ずつ「直した／見送った」と合意に書いたか。
   手つかずの m-12・m-15・m-17 を直すか、次の題として書いたか
4. **M-3**: 受入 4 回目の不合格とその対応をジャーナル（`.pair-agent/sprints/43898a8f-....json`）に書いたか
5. **M-4**: `planner="mechanical"` の問いで注意を出さない（あるいは合意 `:193` の「直した」を実態に合わせる）。
   「読み手は全部同じモデル。」の重複と、読み手 1 体のときの食い違いを直したか
6. **minor m-4**: 1 モデル構成で拒否権が `elif` に吸われて出ないことを、直すか文書に書いたか
