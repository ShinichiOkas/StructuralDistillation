---
sprint_id: 43898a8f-fb98-4ace-a6d3-dc6b1446d0ba
round: 6
date: 2026-09-23
verdict: 合格
counts: {critical: 0, major: 1, minor: 9}
---

# 受入検査報告書（スプリント「単一モデル運用」6 回目）

検査者は作った本人ではない。報告書・合意・コミットメッセージの数字は信じず、
`single_model.json`・`c_samples2/single_model.json`・`w5/w5.json`・3 つの `records.jsonl` から **自分で数え直した**。
`_note_models` は `tests/conftest.py` の `ScriptedReader` で **48 構成**
（generated / stored / given × 読み手 1 体・2 体同モデル・2 体別モデル・偽読み手混在・偽読み手のみ ×
生成器を省く／渡す × 検証役を省く／1 体／2 体／3 体 × 名前衝突 2 種 × 検出項目だけ／混在）を自分で走らせ、
5 回目の一覧と 1 行ずつ突き合わせた。3 つの記録は **84 件すべて、現行ライブラリの `_note_models` を
記録自身の構成で呼び直して `notes` が文字列単位で一致すること**を確かめた。
LLM は 1 度も呼んでいない（すべてキャッシュと台本・純関数の直接呼び出し）。一時ファイルは
`C:/Users/shini/AppData/Local/Temp/claude/s--work-develop-StructuralDistillation/4ec11b76-.../scratchpad/` に置いた。
`.pair-agent/probes/` には 1 バイトも書いていない（`git status --porcelain` が最初から最後まで空であることを確かめた）。

検めた版は `bcd73c8`（`fix: 記録を 3 つとも作り直し、言い切らない形にする（受入 5 回目 C-1）`）。
5 回目からの差分はこの 1 コミットだけである。

## 判定: **合格**（critical 0・major 1・minor 9）

**3〜5 回目の critical と同じ形（「直した」と書いて直していない／直しの過程で別の場所を壊す）は、今回は 1 件も見つからなかった。**
5 回目の C-1・M-1・M-2・M-3・M-4 と m-12〜m-17 は、現物を当たって**全件閉じている**。
重点的に検めた 3 点（記録が本当に現行版か／`w5.json` の実測が戻ったか／注意が消えていないか）は、
いずれも**主張どおり**だった。とくに記録は、言い回しの目視ではなく**現行ライブラリで再現して 84/84 一致**を取った。

落ちなかったが残るのは、**5 回目の minor 9 件の対応／見送りがどこにも記録されていない**こと（major M-1）で、
これは 3 回目 M-8 →4 回目 M-3 →5 回目 M-2 と**同じ形の 4 度目**である。
合意の完了条件「実装で直すべきものが出たら、直すか、次の題として書く」は、これだけが満たされていない。
ただし **今回は「直した」と書いていないものを直したと書いた箇所が無い**ので、critical には当たらない。

---

## 0. 走らせて確かめたこと（指示された 5 本）

| 検め | 主張 | 実測 | 結果 |
|---|---|---|---|
| `.venv/Scripts/python -m pytest -q` | 207 通る | `207 passed, 1 skipped in 2.06s`（skip は `SD_LIVE` 未設定の実接続） | **主張どおり** |
| `tools/conformance_out4.py` | 21/21・21/21・11/11・外れ 0 | ① 一致 21/21・外れ 0・キャッシュ 1941・**実呼び出し 0** ② 一致 21/21・外れ 0・キャッシュ 2289・実呼び出し 0 ③ 一致 11/11・外れ 0・キャッシュ 2184・実呼び出し 0・外した軸 5（4 題材） | **主張どおり** |
| `tools/mutation_check.py` | 生き残り 0 | `生き残り 0: []`。`殺した` の行数を数えて **65**。AST でも `MUTANTS` は 65 要素（`assert src.count(old) == 1` があるので、当たらない変異体は静かに飛ばされず落ちる） | **主張どおり** |
| `w5/threshold_sweep.py` | 6 升目 | 6 行。B s1 15/18・B s2 13/18・C s1 7/18・C s2 7/18・A s1 16/18・A s2 16/18 が κ=0.667 と κ=0.5 で同じ | **主張どおり** |
| `single_model_report.py --run . --w5 w5 --c2 c_samples2` との `diff` | 空 | **空**（`DIFF EMPTY`） | **主張どおり** |

自分で数え直した 6 つの升目（`arm_summary` を使わず、`hit()` の規則と
「NO_EVIDENCE/INSTRUMENT_FAULT の読み手は p に入れない」という報告器の規則を自分で書いて集計）:

```
B s1: 15/18  外した=['m05','m06','m08']
B s2: 13/18  外した=['m04','m05','m08','m14','m18']
A s1: 16/18  外した=['m05','m08']
A s2: 16/18  外した=['m05','m08']
C s1:  7/18  外した=['m01','m01b','m04','m05','m08','m10','m14','m16','m18','m19','m20']
C s2:  7/18  外した=['m01','m01b','m03','m04','m05','m07','m10','m14','m16','m18','m19']

C 反転: ['m03','m07','m08','m20']（s1のみ m03・m07 ／ s2のみ m08・m20）
B 反転: ['m04','m06','m14','m18']
A 反転: []
標本1: 生成器の寄与 B-C = 8  読み手の寄与 A-B = 1
標本2: 生成器の寄与 B-C = 6  読み手の寄与 A-B = 3
m18 標本 2: qwen3.5:4b#a NO_EVIDENCE/有効率 0.333、#b NO_EVIDENCE/有効率 0.167 → 平均 p なし
p の中央値: 題材ごとの平均で 0.833・読み手ごとで 0.817
```

**3・4・5 回目の数え直しと完全に一致**した。記録を 3 つとも作り直しても、測定値は 1 つも動いていない。

---

## critical

**なし。**

今回とくに探した形（過去 3 回の critical）について、探した結果を書く:

| 探した形 | 探し方 | 結果 |
|---|---|---|
| 記録の作り直し忘れ | 3 つの記録 84 件すべてについて、記録自身の構成（`question_set.source`・`planner`・`crosscheck`・`readings` の読み手名）から現行 `_note_models` を呼び直し、`notes` を文字列比較 | **84/84 一致・0 件不一致**。古い言い回し（`読み手と同じ系統かどうか` / `同じなら、判定は 1 モデルで回っている` / `読み手は全部同じモデル`）はリポジトリ全体で 3 記録に **0 件**（残るのは 3・5 回目の報告書が引用している分だけ） |
| 「直した」と書いて直していない | 合意の 5 回目表（C-1・M-1〜M-4）と 4 回目 minor 表（m-12〜m-17）の**全 11 行**について、書かれた先のファイルを開いて字面を確かめた | **11/11 が現物と一致**（下の開閉表） |
| 直しの過程で別の場所を壊す | 48 構成の総当たり／記録と `single_model.json` の突き合わせ（105 件）／`w5.json` の全欄差分／変異試験 65 個／適合検査 3 本 | **壊れは無し**。注意は 1 件も消えていない。測定値は 1 つも動いていない |

---

## major

### M-1 5 回目の minor 9 件の対応／見送りが、合意にもジャーナルにも 1 行も無い（同じ形の 4 度目）

**どこ**: `.pair-agent/agreements/single-model-operation.md:195-217`（5 回目の対応表と「4 回目 minor の 1 件ずつ」）、
`.pair-agent/sprints/43898a8f-fb98-4ace-a6d3-dc6b1446d0ba.json`（2026-09-23T21:30 の経過）

**何が問題か**

5 回目の報告書は `counts: {critical: 1, major: 4, minor: 9}`。
合意には「受入検査 5 回目と対応」表（C-1・M-1〜M-4 の 5 行）と「4 回目 minor の 1 件ずつ」表（m-12〜m-17）は入ったが、
**5 回目の minor 9 件（m-1〜m-9）についての行が 1 つも無い**。現物を当たると:

| 5 回目 | 現状 | 判定 |
|---|---|---|
| m-1（＝4 回目 m-12 名前／モデル名の取り違え・死んだ条件） | `compose.py:188-198` で直った | **直った**（4 回目 m-12 の行として記録あり） |
| m-2（＝4 回目 m-15 空白落ち） | 注意は「想定に合った題材が 7/18 まで落ちた」。記録 21 件もこの形 | **直った**（同上） |
| m-3（＝4 回目 m-17 安い殺し） | `mutation_check.py:60-71` を退行そのものの形に差し替え | **直った**（同上） |
| m-4 1 モデル構成で拒否権の注意が `elif` に吸われて出ない | `compose.py:175-182` そのまま。私の総当たりでも `G 読2(同)・生省略・検省略` は `['自己XC','読同一','生読同一']` で拒否権は出ない | **手つかず・記録なし** |
| m-5 合意 `:64`「約 500 呼び出し」対 probe README `:41`「488 呼び出し」 | 合意 `:64` は「約 500」のまま | **手つかず・記録なし** |
| m-6 上流 `## 変更履歴` に 3〜5 回目の行が無い／v3.8 が無条件の 15/18 | `:554` が「15/18・標本 1」に、`:551-553` に v3.9 が入った | **直ったが記録なし** |
| m-7 合意 `:89` 「3 件」に対し 4 つの値 | そのまま | **手つかず・記録なし** |
| m-8 合意 frontmatter の `version`・`updated_at` が動かない | `version: 3`・`updated_at: 2026-09-23T15:05:00+09:00` のまま。`## 変更ログ` にも行が無い | **手つかず・記録なし** |
| m-9 probe README の `## 再現` に記録の作り直し手順が無い | `:52` に確かめ方（`notes` の言い回し）は入ったが、`## 再現`（`:59-65`）には引き直しコマンドが無いまま | **半分・記録なし** |

つまり **9 件中 4 件は直り、4 件は手つかず、1 件は半分**なのに、**どれについても「直した／見送った」の記録が無い**。
ジャーナルの 2026-09-23T21:30 の経過も「4 回目 minor 6 件（m-12〜m-17）を 1 件ずつ直して合意に開閉を書いた」で止まり、
5 回目の minor には触れていない。

これは 3 回目 M-8 →4 回目 M-3 →5 回目 M-2 と**同じ指摘の 4 度目**である。
5 回目 M-2 の対応として「4 回目 minor の 1 件ずつ」表を作ったのに、**その表を 5 回目の minor には作っていない**。
合意の完了条件「実装で直すべきものが出たら、直すか、次の題として書く」は、これだけが満たされない。

⚠ 4 回目までと違い、**「直した」と書いてあるのに直っていない行は 1 つも無い**。落ちているのは記録の不在だけなので major とした。

**どう確かめたか**

```
$ grep -n "m-1\b\|m-2\b\|m-4\b\|m-5\b\|m-7\b\|m-8\b\|m-9\b" .pair-agent/agreements/single-model-operation.md
（5 回目の minor を指す行は 0 件。当たるのは 1〜3 回目の対応表の m-1・m-2 など別の番号だけ）

$ sed -n '64p'  .pair-agent/agreements/single-model-operation.md
腕 W5 はこれに加えてローカル 1,008 呼び出し・約 38 分、腕 C の標本 2 はローカル 約 500 呼び出し・約 20 分。
$ sed -n '41p'  .pair-agent/probes/single-model-1/README.md
腕 C の標本 2 は `c_samples2/`（488 呼び出し・約 20 分。…）
$ .venv/Scripts/python -c "...c_samples2/single_model.json の cost を合計..."
c_samples2/single_model.json seconds 合計 19.0 分 {'plan': 0, 'crosscheck': 0, 'answer': 488} 合計 488

$ sed -n '1,6p' .pair-agent/agreements/single-model-operation.md
version: 3 / updated_at: 2026-09-23T15:05:00+09:00     ← 3〜6 回目の追記の後も動いていない

$ .venv/Scripts/python .../scratchpad/notes_matrix6.py | grep "生省略・検省略"
  G 読2(同)・生省略・検省略   ['自己XC', '読同一', '生読同一']     ← 拒否権は出ない（m-4 は手つかず）
```

---

## minor

- **m-1**（5 回目 m-7 の再掲・手つかず。**中身も誤り**）: 合意 `:89`
  「うち 3 件は判定の境目（平均 p が **0.3 / 0.4 / 0.6 / 0.7** のすぐ近く）」は、3 件に 4 つの値を並べている。
  `w5.json` から平均 p を計算すると、境目は **0.3（m04: 0.333→0.267・split の下端）・0.4（m06: 0.417→0.375・refute の上端）・
  0.7（m14: 0.633→0.750・split の上端）の 3 つ**で、**0.6 に当たる題材は無い**。
  `report.md:99` は値を書かずに「3 件は判定の境目」とだけ言うので、合意だけが 1 つ余分に持っている。
- **m-2**（5 回目 m-5 の再掲・手つかず）: 合意 `:64`「腕 C の標本 2 はローカル **約 500 呼び出し**」対
  probe `README.md:41`「**488 呼び出し**」・`report.md:139`「回答 488」。同じ量が 2 通り（実測は 488）。
- **m-3**（5 回目 m-4 の再掲・手つかず）: 1 モデル構成では `compose.py:175-182` が `elif` なので、
  自己交差検証の注意が出て**拒否権の注意は出ない**。拒否権は 1 モデルのときにも成り立つ事実である
  （既定の検証役 ＝ 偽でない読み手全員・生成器 ＝ `real[0]` なので、読み手 2 体の既定呼び出しでは必ず成立する）。
  上流 `:444-445` と README `:166-167` は「モデルが別々でも出る」と書くが、「1 モデルのときは出ない」には触れていない。
- **m-4**（5 回目 m-8 の再掲・手つかず）: 合意の frontmatter が `version: 3`・`updated_at: 2026-09-23T15:05:00+09:00` のまま。
  3〜6 回目の対応表を追記した後も動いておらず、`## 変更ログ` にも行が無い。
- **m-5**（**新規**・M-1 の対応で入った書き方の弱さ）: `w5/w5.json` の実測を守る仕組みは**文書だけ**で、道具には無い。
  `w5/w5_run.py:91-119` は `mid in out and not args.force` のときに飛ばすので**素の引き直しでは保たれる**が、
  `--force` か新しい `--out` で走らせると `row` を作り直し（`:97`）、`row["seconds"] = round(time.time()-t0, 1)`（`:118`）・
  `cost_s{samples}`（`:108`）を上書きし、**`_note` を書く行がどこにも無いので `_note` ごと消える**。
  5 回目 M-1 の事故（`strong_readers_s2` を足すために全題材を引き直した）はまさにこの経路で起きた。
  ⚠ 合意 `:203`・README `:56-57` はどちらも「明記した」「保つこと」と書いており、**道具を直したとは書いていない**ので、
  主張と実体の食い違いではない。次に欄を足すときに同じことが起きる、という指摘。
- **m-6**（**新規**・`_note` の言い過ぎ）: `w5.json` の各行の `_note` は
  「`seconds` と `cost_*` は 2026-09-23 の**本走行の実測**」と書くが、`cost_*` に含まれる **`strong_cost_s2` はこの commit で
  新設された欄**で、本走行には存在せず、中身も `{'live': 0, 'cached': 1008}`（引き直しの値）である。
  守るべきは `seconds`・`cost_s1`・`cost_s2` の 3 つで、README `:56` の「この 3 欄」の方が正しい。
  ```
  $ .venv/Scripts/python -c "...w5.json の cost を欄ごとに合計..."
  cost_s1        {'live': 0,    'cached': 504,  'missed': 0}
  cost_s2        {'live': 1008, 'cached': 0,    'missed': 0}
  strong_cost_s1 {'live': 0,    'cached': 504,  'missed': 0}
  strong_cost_s2 {'live': 0,    'cached': 1008, 'missed': 0}   ← 本走行には無かった欄
  ```
- **m-7**（5 回目 m-9 の残り半分）: probe `README.md:52` に確かめ方（「記録の `notes` に古い言い回しが残っていないか」）は入ったが、
  **古い言い回しの一覧がどこにも無い**ので機械的には回せない（今回私が使ったのは「記録自身の構成で `_note_models` を
  呼び直して文字列比較する」方法で、こちらは一覧が要らない）。`## 再現`（`:59-65`）にも記録の作り直しコマンドは無いまま。
- **m-8**（**新規**）: 変異体は 64 → **65** になった（4 個を外し 5 個を足した）が、**現在の数を書いた文書が無い**。
  合意 `:177` の「53 → 57 → 60 → **64**（今回 +4）」は 3 回目時点の記述として正しいので誤りではないが、
  3 回目に「変異体の数が誤り」（major 3）で叱られた量なのに、今回の増減はコミットメッセージにも数として出ていない。
- **m-9**（**新規**・記録の欠落）: `.pair-agent/acceptance/` には 2〜5 回目の報告書しか無く、**1 回目の報告書が無い**
  （合意 `:130` の「受入検査 1 回目（不合格・critical 4）」だけが 5 つの節のうち唯一、報告書のパスを挙げていない）。
  `git log --all -- .pair-agent/acceptance/*` にも現れない。
- **m-10 相当の観察**（指摘ではなく注記）: `.pair-agent/probes/impl-v1-live/live_records.jsonl`（2 件）は
  `notes` 欄そのものが無い旧スキーマの記録で、現行ライブラリで同じ構成を走らせると拒否権の注意が 1 件出る。
  probe README の「3 つの記録」の主張の外なので**指摘には数えない**が、リポジトリには現行版で作れない記録が 1 つ残っている。

---

## 5 回目の指摘の開閉（1 件ずつ）

| # | 5 回目の指摘 | 開閉 | 根拠 |
|---|---|---|---|
| **C-1** | `c_samples2/records.jsonl` だけ旧版のライブラリ製なのに probe README は「3 つとも現行版で作り直してある」と書いていた | **閉じた** | 3 つの記録はすべて `2026-09-23T17:43:4x`（commit の 4 分前）に作り直されている。**84 件すべて**について、記録自身の構成から現行 `_note_models` を呼び直し、`notes` が文字列単位で一致（`再現できた 84・できなかった 0`）。古い言い回しは 3 記録に 0 件。測定値は**元の走行の記録**（`0956a5f`・`8fdc1a4`・`f551811`）と `at`・`cost`・`notes`・`matrices[].answers[].cached` を除いて **21/21・42/42・21/21 で完全一致**し、`cost.live` は 84 件すべて 0。probe `README.md:50-54` の文はこの実測と合っている |
| **M-1** | `w5/w5.json` の実測（`seconds` 38.5 分・`live` 1,008）を引き直しで 0 に上書きした | **閉じた** | `seconds` 合計 **38.5 分**・`cost_s1`+`cost_s2` の `live` 合計 **1,008** に戻っている（`8fdc1a4`・`1bc0232` と同値）。`1bc0232` との全欄突き合わせで、**違う欄は `notes` だけ**、増えた欄は `strong_readers_s2`・`strong_delta_s2`・`strong_cost_s2`・`_note` の 4 つ。つまり引き直しで得た欄（`strong_*_s2`）は残り、実測の欄だけが戻っている。probe `README.md:56-57` と合意 `:203` に経緯も書かれた。⚠ 守る仕組みは文書だけ（minor m-5・m-6） |
| **M-2** | 4 回目 minor（m-12〜m-17）の対応／見送りの記録が無い | **閉じた** | 合意 `:208-217` に「### 4 回目 minor の 1 件ずつ」表（6 行）。6 件とも現物を当たって**実際に直っている**（下表） |
| **M-3** | 受入 4 回目とその対応がジャーナルに無い | **閉じた** | `経過` は 10 → **12 件**。`2026-09-23T20:20`（4 回目・critical 1・major 3・minor 12）と `2026-09-23T21:30`（5 回目・critical 1・major 4・minor 9）が入った。件数は 4 回目・5 回目の報告書の frontmatter と一致する |
| **M-4** | 3 回目 m-4（機械的な問いへの偽陽性）を「直した」と書いたが条件は変えていない。足した「読み手は全部同じモデル。」が重複と 1 体時の食い違いを生んだ | **閉じた** | 条件に `and any(a.kind != "detection" for a in qs.axes)` が入り（`compose.py:194`）、`planner="mechanical"`・検出項目だけの問いでは出所の注意が**出なくなった**（`stored/given 検出項目だけ` → `['読同一']` のみ）。前置き「読み手は全部同じモデル。」は外れ、**読み手 1 体のときの食い違いも 2 度言いも消えた**（`S 再利用・読1(実)` → `読1体` ＋ 名前告知で、注意文は「この問いの集合を作った生成器は…」から始まる）。変異体「judge 検出項目だけの問いにも出所を言う」も追加され殺されている |
| **m-12** | 再利用で名前の一致から誤断定する／条件の後半が死んでいた | **閉じた** | 死んだ条件（`qs.planner in models and any(model_of(r) == qs.planner for r in real)`）は**丸ごと消えた**。言い切るのは `generated and planner is not None and model_of(planner) in models` のときだけ。名前衝突の総当たり（生成器名＝読み手のモデル名・実体は別モデル）は、5 回目の `読同一・生読同一（誤断定）` から **`読同一・名前告知・推測`**（「…可能性が高い」）に変わり、**誤断定が消えた**。変異体「judge 再利用でも名前の一致で言い切る」「judge 名前が一致しても手がかりを添えない」も追加され殺されている |
| **m-13** | `compose.py` のドックストリングが無条件の 15/18 | **閉じた** | `:155`「強いモデルが作った軸なら **13〜15/18** だった（標本数で動く）」 |
| **m-14** | 上流 §13 の「15/18 を当てた」が無条件 | **閉じた** | `doc/UPSTREAM_DESIGN.md:532`「同じ弱い読み手は強い軸なら **13〜15/18** を当てた。標本数で動く」。`grep -rn "15/18" --include=*.md .` で無条件のまま残る箇所は無い（`:554` の v3.8 も「15/18・標本 1」に） |
| **m-15** | 注意の文面で「題材が7/18」と空白が落ちている | **閉じた** | 現行の注意は「想定に合った題材が **7/18** まで落ちた」。本走行の記録 21 件もこの形で載っている |
| **m-16** | 上流 §10 の「p の中央値 0.83」に定義が無い | **閉じた** | `:415`「p の中央値は 題材ごとの平均で **0.833**・読み手ごとで **0.817**」。私が `single_model.json` から計算した値（0.833・0.817）と一致 |
| **m-17** | 足した変異体 2 個が 3 回目の退行そのものの形を模していない（安い殺し） | **閉じた** | 「既定の生成器／検証役を注意の対象にしない」（生成そのものを壊す形）を外し、**注意の呼び出しにだけ利用側の引数を渡す形**（`_note_models(..., planner, ...)` / `..., list(verifiers or []), ...`）に差し替えた。これは 3 回目 C-1 の退行そのもの。どちらも `殺した` |

### 4 回目 minor 6 件の現物（M-2 の裏取り）

| 4 回目 | 現物 | 判定 |
|---|---|---|
| m-12 名前／モデル名の取り違え・同値な 2 条件 | `compose.py:188-198`。死んだ条件は削除、言い切りは generated 限定、再利用は「可能性が高い」 | **閉じた** |
| m-13 ドックストリング 15/18 | `compose.py:155` 13〜15/18 | **閉じた** |
| m-14 上流 `:525` の無条件 15/18 | `:532` 13〜15/18・標本数で動く | **閉じた** |
| m-15 「題材が7/18」の空白落ち | `compose.py:192`「題材が 7/18 まで落ちた」 | **閉じた** |
| m-16 上流 `:415` の p の中央値の定義 | `:415` 題材ごと 0.833・読み手ごと 0.817 | **閉じた** |
| m-17 変異体 2 個の安い殺し | `tools/mutation_check.py:60-71` を退行そのものの形に差し替え | **閉じた** |

---

## 特に検めてほしかった 4 点（依頼文）への回答

### ① 3 つの記録が本当に現行版のライブラリで作られているか

**主張どおり。**目視ではなく、**記録自身の構成から現行ライブラリを呼び直して文字列比較**した。

```
$ .venv/Scripts/python .../scratchpad/（記録の question_set・readings から _note_models を呼び直す）
現行ライブラリで再現できた記録: 84  再現できなかった: 0  （合計 84）
```

3 つの記録に載っている注意の集合（全件走査・重複を畳んだもの）:

```
records.jsonl（本走行・source=generated・planner qwen3.5:4b#plan・crosscheck あり）:
  [21] 検証役が生成器と同じモデル（自己交差検証）…
  [21] 読み手が全部同じモデル（qwen3.5:4b）…
  [21] 生成器も読み手も同じモデル（qwen3.5:4b）。…想定に合った題材が 7/18 まで落ちた（…13〜15/18。標本数で動く）
w5/records.jsonl（source=given・planner gemma4:31b-cloud・crosscheck なし・budget.crosscheck=False）:
  [42] 読み手が全部同じモデル（qwen3.5:4b）…
  [42] この問いの集合を作った生成器は gemma4:31b-cloud・交差検証なし（記録に残った名前）。名前しか残らないので、…
c_samples2/records.jsonl（source=stored・planner qwen3.5:4b#plan・crosscheck あり）:
  [21] 読み手が全部同じモデル（qwen3.5:4b）…
  [21] この問いの集合を作った生成器は qwen3.5:4b#plan・検証役は qwen3.5:4b#v1・qwen3.5:4b#v2（記録に残った名前）。…
```

どれも現行の枝の出力と一致する。`qs.planner`（`qwen3.5:4b#plan` / `gemma4:31b-cloud`）は読み手のモデル名
（`qwen3.5:4b`）と一致しないので、「可能性が高い」の一節が**正しく付いていない**ことも確かめた。

古い言い回しの残留:

```
$ grep -c "読み手と同じ系統かどうか\|同じなら、判定は 1 モデルで回っている\|読み手は全部同じモデル" \
    .pair-agent/probes/single-model-1/{records.jsonl,w5/records.jsonl,c_samples2/records.jsonl}
0 / 0 / 0
$ grep -rln "…（同じ 3 語）…" . | grep -v "^./.git/"
./.pair-agent/acceptance/43898a8f-single-model-3.md
./.pair-agent/acceptance/43898a8f-single-model-5.md      （過去の報告書が引用している分だけ）
```

測定値が前の版と一致するか（`git show` で突き合わせ）:

| 記録 | 元の走行の版 | 件数 | `at`・`cost`・`notes`・`answers[].cached` を除いた一致 | `cost.live` |
|---|---|---|---|---|
| `records.jsonl` | `0956a5f` | 21 → 21 | **21/21** | 0 |
| `w5/records.jsonl` | `8fdc1a4` | 42 → 42 | **42/42** | 0 |
| `c_samples2/records.jsonl` | `f551811` | 21 → 21 | **21/21** | 0 |

直前の版（`349e7d7`）との差分は **`notes` と `at` だけ**（3 記録とも）。`matrices`・`readings`・`summary`・
`question_set`・`input` は 1 件も動いていない。記録と `single_model.json` の読みも突き合わせて **105/105 一致・食い違い 0**。

### ② `w5/w5.json` の `seconds`・`cost_s1`・`cost_s2` が実測に戻っているか。他の欄が引き直しの値か

**主張どおり。**

```
$ .venv/Scripts/python .../scratchpad/（各版の w5.json を合計）
8fdc1a4    題材 21  seconds合計 38.5 分  live合計 1008  cached合計 504
1bc0232    題材 21  seconds合計 38.5 分  live合計 1008  cached合計 504
349e7d7    題材 21  seconds合計  0.0 分  live合計    0  cached合計 1512   ← 5 回目 M-1 の状態
HEAD       題材 21  seconds合計 38.5 分  live合計 1008  cached合計 504    ← 戻っている
```

全欄の突き合わせ:

```
HEAD 対 1bc0232（実測が生きていた最後の版）
  鍵一致: True
  増えた欄: {'strong_readers_s2': 21, 'strong_delta_s2': 21, 'strong_cost_s2': 21, '_note': 21}
  消えた欄: {}
  違う欄:   {'notes': 21}

HEAD 対 349e7d7（実測が消えていた版）
  増えた欄: {'_note': 21}
  違う欄:   {'cost_s2': 21, 'seconds': 21, 'notes': 21}
```

→ **引き直しで得た欄（`strong_readers_s2`・`strong_delta_s2`・`strong_cost_s2`）は残り、実測の欄（`seconds`・`cost_s2`）だけが戻っている。**
`strong_readers_s2` は私の独立集計でも A s2 ＝ 16/18（外したのは m05・m08。標本 1 と同じ題材）で、定数ではなく記録から来ている。
`_note` は 21 行すべてに入っている（⚠ その文面は minor m-6、守る仕組みが文書だけなのは minor m-5）。

### ③ `_note_models` の書き換えで注意が消えていないか（総当たり）

`ScriptedReader` で **48 構成**を自分で走らせ（LLM 呼び出し 0）、5 回目の報告書の一覧と 1 行ずつ突き合わせた。
略号 ＝ XC無・自己XC・拒否権・読1体・読同一・生読同一・名前告知・推測（「可能性が高い」）・偽のみ。

| 構成 | 5 回目 | いま | 判定 |
|---|---|---|---|
| **G** 読2(同)・生省略・検省略／生渡す・検省略／生省略・検2渡す／生渡す・検2渡す | 自己XC・読同一・生読同一 | 同じ | **消えていない** |
| G 読2(同)・生渡す・検1渡す | XC無・読同一・生読同一 | 同じ | 消えていない |
| G 読1(実)・省略/省略 | XC無・読1体・生読同一 | 同じ | 消えていない |
| G 読1(実)・生渡す・検2渡す | 自己XC・読1体・生読同一 | 同じ | 消えていない |
| G 読2(**異**)・省略/省略 | 拒否権 | 拒否権 | 消えていない |
| G 読2(異)・生渡す(第3のモデル)・検省略 | （なし） | （なし） | 増えていない |
| G 読2(同)+偽1／+偽2／偽が先頭 | 自己XC・読同一・生読同一 | 同じ | 消えていない |
| G 読1(実)+偽1 | XC無・読1体・生読同一 | 同じ | 消えていない |
| G 偽のみ | 偽のみ | 偽のみ | 消えていない |
| G 生成器が検証役2の1体を兼ねる（読み手別モデル） | 拒否権 | 拒否権 | 消えていない |
| G 生成器が検証役**3**の1体を兼ねる | （なし） | （なし） | 正しい（`l1.py:174` の `opposing * 2 > len(verifiers)`。3 体なら 2 票で外れる） |
| G 読2(同)・検証役だけ別モデル2体 | 読同一・生読同一 | 同じ | 消えていない |
| G 交差検証OFF(budget) | 読同一・生読同一 | 同じ | 消えていない |
| **S** 再利用・読2(同)・別系統の生成器名 | 読同一・名前告知 | 同じ | 消えていない（⚠ 文面から「読み手は全部同じモデル。」が外れた＝ 5 回目 M-4 の 2 度言いが解消） |
| S 再利用・読1(実) | 読1体・名前告知 | 同じ | 消えていない（⚠ 1 体なのに「全部同じモデル」と言う食い違いも解消） |
| S 再利用・読2(異) | （名前告知なし） | （なし） | 変わらず |
| S 再利用・読2(同)+偽1／検2渡す | 読同一・名前告知 | 同じ | 消えていない |
| S 再利用・偽のみ／偽1体 | 偽のみ | 偽のみ | 消えていない |
| S 再利用・`c_samples2` の実構成（`#plan`・`#v1`/`#v2`） | 読同一・名前告知 | 同じ | 消えていない（記録 21 件と一致） |
| **D** 渡す・読2(同)・別系統／読1(実)／+偽2 | 読同一 or 読1体 ＋ 名前告知 | 同じ | 消えていない |
| D 渡す・読2(異) | （なし） | （なし） | 変わらず |
| D 渡す・偽のみ2体／偽1体 | 偽のみ | 偽のみ | 消えていない |
| D 渡す・交差検証なしの問い・読2(同)／読1 | XC無 ＋ 読同一/読1体 ＋ 名前告知 | 同じ | 消えていない |
| D 名前衝突（記録の生成器名＝読み手の**名前**・実体別） | 読同一・名前告知 | 同じ | 正しく言い切らない |
| **D／S 名前衝突（記録の生成器名＝読み手の<br>モデル名・実体別）** | 読同一・**生読同一**（誤断定） | 読同一・名前告知・**推測** | **誤断定が消えた**（4 回目 m-12 が閉じた） |
| **S／D 検出項目だけ（`planner="mechanical"`）・読2(同)** | 読同一 ＋ **名前告知**（無意味） | **読同一** のみ | **偽陽性が消えた**（3 回目 m-4 が閉じた） |
| S／D 検出項目だけ・読1 | 読1体 ＋ 名前告知 | **読1体** のみ | 同上 |
| S／D 検出項目だけ・生成器名＝読み手のモデル名 | 読同一・生読同一 | **読同一** のみ | 同上 |
| S／D 検出項目＋実質の軸の混在 | — | 読同一・名前告知 | **正しい**（実質の軸があるなら出所は意味を持つ） |
| G 検出項目だけ（generated） | 読同一・生読同一 | 同じ | 変わらず（`l1.plan` は `kind="detection"` を作らないので、実際には起きない構成） |

**結論: 消えた注意は 1 件も無い。変わったのは 5 回目で指摘された 2 か所（検出項目だけの問いへの偽陽性と、
名前の一致からの誤断定）だけで、どちらも「余分に出ていたものが出なくなった／言い切りが弱まった」方向である。**

退行の検出力も落ちていない。3 回目の退行そのものの形（注意にだけ利用側の生の引数を渡す）は、
今回**変異体として `mutation_check.py` に入った**（`judge 注意にだけ既定の生成器を渡さない` /
`… 既定の検証役を渡さない`）。どちらも `殺した`。新しい枝も 3 個の変異体
（`検出項目だけの問いにも出所を言う` / `再利用でも名前の一致で言い切る` / `名前が一致しても手がかりを添えない`）で守られ、
65 個すべて死ぬ。

### ④ 5 文書（合意・`report.md`・上流設計・README・probe README）の食い違い

| 事実 | 合意 | `report.md` | 上流 | README | probe README | 判定 |
|---|---|---|---|---|---|---|
| **記録は現行版で作り直し** | — | — | — | — | `:50-54` ○ | **揃った**（84/84 で再現。**C-1 は閉じた**） |
| **W5 は 1,008 呼び出し・約 38 分** | `:64` ○ | `:142`（1008・キャッシュ行数から独立に） | — | — | `:40`・`:56` ○ | **揃った**（`w5.json` の実測が裏づけになった。**M-1 は閉じた**） |
| **腕 C の標本 2 は 488 呼び出し** | `:64`「**約 500**」✗ | `:139`「488」 | — | — | `:41`「488」 | **minor m-2**（実測は 488） |
| **腕 C も 4 題材入れ替わる** | `:88` ○ | `:103` ○ | `:420` ○ | — | — | **揃っている** |
| **m18 は境目でなく有効率の崩れ** | `:90` ○ | `:104` ○ | `:421` ○ | — | — | **揃っている** |
| **B で反転した 3 件の境目の値** | `:89`「0.3/0.4/**0.6**/0.7」✗ | `:99`（値を書かない） | — | — | — | **minor m-1**（実測は 0.3・0.4・0.7） |
| **13〜15/18**（標本で動く） | `:79`・`:99` ○ | `:88`・`:100` ○ | `:418`・`:532`・`:554` ○ | `:161` ○ | `:35` ○ | **揃った**（5 回目 m-6 も閉じた） |
| `compose.py` のドックストリング | — | — | — | — | — | `:155` 13〜15/18 ○（4 回目 m-13） |
| **p の中央値の定義** | — | `:45` ○ | `:415` ○ | — | — | **揃った**（私の計算 0.833／0.817 と一致） |
| **非排他 5.6 倍** | `:119`・`:142`・`:160` ○ | `:68` ○ | `:437`・`:524` ○ | — | — | **揃っている** |
| **拒否権** | — | — | `:444` ○ | `:166-167` ○ | — | **揃っている**。⚠ どちらも「1 モデルのときは出ない」に触れない（**minor m-3**） |
| **変更履歴に 3〜5 回目の行** | — | — | `:551-553`（v3.9）○ | — | — | **揃った**（5 回目 m-6） |
| 合意の frontmatter | `version: 3`・`updated_at: 15:05` ✗ | — | — | — | — | **minor m-4** |
| `CLAUDE.md` と `GEMINI.md` | — | — | — | — | — | `diff` が**空**（同期されている） |

---

## 合意の `## 完了条件` を 1 件ずつ

| 完了条件（`:219-224`） | 判定 | 根拠 |
|---|---|---|
| 4 腕の記録がリポジトリに取り込まれ、判断規則に照らした読みが合意に書かれている | **満たす** | 記録・走行器・読み取り器がそろい、`report.md` は引き直しと**完全一致**（`diff` 空）。3 つの記録は現行ライブラリで **84/84 再現**でき、測定値は元の走行と 21/21・42/42・21/21 で一致。`w5.json` の実測も戻った。合意 `:87-91` の読み（B−C ＝ +8/+6、A−B ＝ +1/+3、C は 4 題材入れ替え、m18 は有効率の崩れ）は私の独立集計と全一致 |
| 上流設計に「1 モデルのとき何を保証し、何を保証しないか」が書かれている（§10 の適用限界） | **満たす** | `:415-424` に 2×2・p の中央値の定義・揃っていない 3 つとその確かめ方、`:420-421` に腕 C の入れ替えと m18、`:441-445` に注意の枠（1 モデル運用のもの／それとは別のもの）、`:532` に 13〜15/18、`:551-553` に v3.9 の変更履歴 |
| 実装で直すべきものが出たら、直すか、次の題として書く | **満たさない** | 5 回目の minor のうち **m-4（1 モデルで拒否権が出ない）・m-5（約 500 対 488）・m-7（3 件に 4 値）・m-8（frontmatter）** は、**直してもいないし次の題としても書いていない**（**major M-1**）。⚠ 4 回目の minor（m-12〜m-17）は 6 件とも直り、合意に開閉が書かれた |
| 受入で critical 0 | **満たす** | **critical 0** |

---

## 確かめた結果、主張どおりだったもの（沈黙で済ませない）

| 主張 | 自分で確かめた値 | 判定 |
|---|---|---|
| pytest 207 通る | `207 passed, 1 skipped in 2.06s` | **主張どおり** |
| 適合検査 21/21・21/21・11/11・外れ 0 | すべて一致・実呼び出し 0・外した軸 5（4 題材） | **主張どおり** |
| 変異体 生き残り 0 | `生き残り 0: []`。`殺した` 65 行・AST でも 65 要素。`assert src.count(old) == 1` があるので飛ばされた変異体は無い | **主張どおり** |
| `threshold_sweep.py` が 6 升目を出す | 6 行。C の標本 2 も両閾値で 7/18 | **主張どおり** |
| 引き直しと `report.md` が完全一致 | `diff` が空 | **主張どおり** |
| 3 つの記録が現行版・実呼び出し 0・測定値不変 | 現行ライブラリで **84/84 再現**。元の走行と 21/21・42/42・21/21 一致。`cost.live` は 84 件すべて 0 | **主張どおり** |
| `w5.json` の `seconds` 38.5 分・`live` 1,008 | 合計して一致。`1bc0232` と同値 | **主張どおり** |
| `strong_readers_s2` などが引き直しの値 | `1bc0232` に無く HEAD にある欄。A s2 は私の独立集計でも 16/18 | **主張どおり** |
| 注意が 1 件も消えていない | 48 構成の総当たりで、5 回目の一覧と過不足なし | **主張どおり** |
| 再利用では言い切らない／名前の一致は「可能性が高い」 | 名前衝突の総当たりで誤断定が消え、`推測` に変わった | **主張どおり** |
| 検出項目だけの問いには出所の注意を出さない | `stored`・`given` × 読み手 2 体／1 体／名前一致 の 6 構成すべてで出ない。実質の軸が混じるときは出る | **主張どおり** |
| 2×2 の 6 升（16/15/7 と 16/13/7）・寄与 +8/+1 と +6/+3 | 自分で集計して全一致。R-g の読みも 標本 1「生成器」・標本 2「両方」 | **主張どおり** |
| 腕 C の 4 題材入れ替え（m03・m07 が外れ、m08・m20 が当たる） | 完全一致 | **主張どおり** |
| m18 は標本 2 で有効率 0.333／0.167 の崩れ | `w5.json` から再計算して一致 | **主張どおり** |
| p の中央値 0.833／0.817 | `single_model.json` から計算して一致 | **主張どおり** |
| `single_model.json`（本走行・`c_samples2`）を上書きしていない | `git show --stat bcd73c8` に現れない。`seconds` 合計は 71.0 分・19.0 分で README `:24`・`:43` と一致 | **主張どおり** |
| 記録と `single_model.json` の読みが整合 | 21 題材 × 5 読み手 ＝ **105/105 一致・食い違い 0** | **主張どおり** |
| ジャーナルに 4 回目・5 回目 | `経過` 12 件。件数（1/3/12・1/4/9）も報告書の frontmatter と一致 | **主張どおり** |
| `CLAUDE.md` と `GEMINI.md` の同期 | `diff` が空 | **主張どおり** |
| 1〜5 回目で確認済みの数字（R-a 7/18・R-d 適合率 1.00/再現率 0.27・p≥0.8 で想定が支持でない 6/18・他系統を借りても 7/18・弱率 0.944/0.849/0.698・非排他 5.6 倍・札が 7/21 で割れ p 同じ 4 件） | `report.md` を引き直して全一致。入力（`matrices[].answers`）が元の走行と 1 件も違わないことも確かめた | **壊れていない** |
| 作業ツリーを汚していない | `git status --porcelain` が検査の前後とも空 | **主張どおり** |

---

## 次に見るところ（合格だが残したもの）

1. **M-1**: 5 回目の minor 9 件を 1 件ずつ「直した／見送った」と合意に書く。
   手つかずの m-4（1 モデルで拒否権が出ない）・m-5（約 500 対 488）・m-7（0.6 は境目でない）・m-8（frontmatter）を、
   直すか次の題として書く。**この形の指摘は 4 回連続**なので、「受入のたびに minor の表を 1 つ足す」を手順にすると切れない
2. **minor m-5**: `w5_run.py` が `seconds`・`cost_s1`・`cost_s2`・`_note` を引き継ぐようにする（いまの守りは文書だけで、
   `--force` か新しい `--out` で `_note` ごと消える）
3. **minor m-6**: `_note` の「`cost_*`」を「`cost_s1`・`cost_s2`」に絞る（`strong_cost_s2` は本走行の実測ではない）
4. **minor m-8**: 変異体の現在数（65）をどこかに書く
