---
sprint_id: 43898a8f-fb98-4ace-a6d3-dc6b1446d0ba
round: 4
date: 2026-09-23
verdict: 不合格
counts: {critical: 1, major: 3, minor: 12}
---

# 受入検査報告書（スプリント「単一モデル運用」4 回目）

検査者は作った本人ではない。報告書・合意・コミットメッセージの数字は信じず、
`single_model.json`・`c_samples2/single_model.json`・`w5/w5.json`・`records.jsonl`・`out4/xc2/crosscheck.json` から
**自分で数え直した**。`_note_models` は `tests/conftest.py` の `ScriptedReader` で
**37 通りの構成**（generated / stored / given × 読み手 1 体・2 体同モデル・2 体別モデル・偽読み手混在・偽読み手のみ ×
生成器を省く／渡す × 検証役を省く／1 体／2 体／3 体）を自分で走らせ、出た注意の一覧を作った。
3 回目の退行そのものの形も**自分で変異体にして**走らせた。
LLM は 1 度も呼んでいない（すべてキャッシュと台本）。一時ファイルは
`C:/Users/shini/AppData/Local/Temp/claude/s--work-develop-StructuralDistillation/4ec11b76-.../scratchpad/` に置いた。
`.pair-agent/probes/` には 1 バイトも書いていない。

## 判定: **不合格**（critical 1・major 3・minor 12）

critical の一行:

- **C-1**（**3 回目と同じ形**）: 3 回目 major 1・2 が指摘した 2 つの誤った記述が、`合意:87-88` と `上流:419` に
  **そのまま残っている**。一方で合意の 3 回目対応表（`:171-172`）は「報告と**合意**を…直した」「**書き分けた**」と書いている。
  ＝ **自分の文書について、していない対応を「した」と書いている**（3 回目 C-2 と同じ「閉じたつもりで閉じていない」）。
  直っているのは `report.md` と `single_model_report.py` の 2 つだけで、4 文書のうち 2 つが手つかず。

**コードは合格水準**。3 回目 C-1 の退行は閉じており、私が退行そのものの形を変異体にしても新しいテストが殺した。
C-2 も閉じている。落ちているのは**文書の整合**である。

---

## 0. 走らせて確かめたこと（指示された 5 本）

| 検め | 主張 | 実測 | 結果 |
|---|---|---|---|
| `.venv/Scripts/python -m pytest -q` | 207 通る | `207 passed, 1 skipped in 1.78s`（skip は `SD_LIVE` 未設定の実接続） | **主張どおり** |
| `tools/conformance_out4.py` | 21/21・21/21・11/11・外れ 0 | ① 一致 21/21・外れ 0・キャッシュ 1941・**実呼び出し 0** ② 一致 21/21・外れ 0・キャッシュ 2289・実呼び出し 0 ③ 一致 11/11・外れ 0・キャッシュ 2184・実呼び出し 0・外した軸 5（4 題材） | **主張どおり** |
| `tools/mutation_check.py` | 生き残り 0・64 個 | `生き残り 0: []`。`殺した` の行数を数えて **64**。AST でも `MUTANTS` は 64 要素 | **主張どおり** |
| `w5/threshold_sweep.py` | 閾値を入れ替えた 2×2 | B s1 15/18・B s2 13/18・C s1 7/18・A s1 16/18・A s2 16/18 が κ=0.667 と κ=0.5 で同じ。**C の標本 2 の行は今回も出ない**（下記 m-1） | **出た数字は主張どおり** |
| `single_model_report.py --run . --w5 w5 --c2 c_samples2` | 報告の引き直し | 差は**末尾の空行 1 行だけ**（`143d142 <`）。本文は `report.md` と一致 | **ほぼ主張どおり**（下記 m-7） |

自分で数え直した 5 つの腕（`arm_summary` を使わず、`hit()` の規則と「NO_EVIDENCE/INSTRUMENT_FAULT の読み手は p に入れない」
という報告器の規則を自分で書いて集計）:

```
C s1: 7/18   外した=['m01','m01b','m04','m05','m08','m10','m14','m16','m18','m19','m20']
C s2: 7/18   外した=['m01','m01b','m03','m04','m05','m07','m10','m14','m16','m18','m19']
B s1: 15/18  外した=['m05','m06','m08']
B s2: 13/18  外した=['m04','m05','m08','m14','m18']
A s1: 16/18  外した=['m05','m08']
C 反転: ['m03','m07','m08','m20']   B 反転: ['m04','m06','m14','m18']
```

3 回目の報告書の数え直しと**完全に一致**した（＝ 3 回目 major 1・2 の指摘は正しい）。

---

## critical

### C-1 3 回目 major 1・2 の誤りが合意と上流に残ったまま、対応表は「直した」と書いている

**どこ**

- `.pair-agent/agreements/single-model-operation.md:87`
  「⚠ **標本数で読みが変わる**（受入 2 回目 C-1）。**動くのは腕 B だけで、A（16/18）も C（7/18）も標本 1・2 で同じ。**」
- 同 `:88`
  「B で反転したのは m04・m06・m14・m18 の 4 題材で、**いずれも判定の境目**（平均 p が 0.3 / 0.4 / 0.6 / 0.7 のすぐ近く）にある。」
- `doc/UPSTREAM_DESIGN.md:419`
  「⚠ **動くのは中央の腕だけで、反転した 4 題材はすべて判定の境目。**」
- 対応表 `.pair-agent/agreements/single-model-operation.md:171`
  「| major 1 | 「動くのは腕 B だけ」は誤り…| 自分で数え直して一致 | **報告と合意を**「数は動かないが中身は入れ替わる」**に直した** |」
- 同 `:172`
  「| major 2 | …m18 は…平均 p が存在しない | 記録で確認 | **3 件は境目・m18 は別の壊れ方、と書き分けた** |」

**何が問題か**

3 回目の再受入項目 3 は「**4 文書とも**書き直したか」だった。実際に直ったのは

- `.pair-agent/probes/single-model-1/report.md:103-104`（正しく書き直されている）
- `.pair-agent/probes/single-model-1/single_model_report.py:345-349`（同）

の 2 つだけで、**合意と上流は 1 文字も動いていない**（`git diff e2fadba HEAD -- doc/UPSTREAM_DESIGN.md` は
`:427` の 7/21 の 1 行だけ。合意の差分も `:122` と対応表の追記だけ）。

しかも合意 `:171` は「報告と**合意**を…直した」と明記している。対応表が**自分の文書についての事実を誤って書いている**。
3 回目 C-2 で叱られたのは「閉じたつもりで閉じていない」ことで、今回は同じことが**文書側で**起きた。
上流設計は `CLAUDE.md` が「上流設計は `doc/UPSTREAM_DESIGN.md`」と定めた参照先なので、
ここに残る誤りは下流のすべての読みに伝播する。

加えて合意 `:87` の「A も C も標本 1・2 で同じ」は、腕 A については題材まで一致する（両方 m05・m08 を外す）が、
腕 C は 4 題材が入れ替わる。**A と C を同じ「同じ」でまとめる**誤りも 3 回目の指摘どおり残っている。

**どう確かめたか**

```
$ grep -rn "動くのは" --include=*.md . | grep -v acceptance
./.pair-agent/agreements/single-model-operation.md:87:⚠ **標本数で読みが変わる**…動くのは腕 B だけで、A（16/18）も C（7/18）も標本 1・2 で同じ。
./.pair-agent/agreements/single-model-operation.md:171:| major 1 | 「動くのは腕 B だけ」は誤り…| 報告と合意を「数は動かないが中身は入れ替わる」に直した |
./.pair-agent/probes/single-model-1/report.md:103:- 数が動くのは腕 B だけ…⚠ ただし **C も中身は 4 題材入れ替わっている**（m03・m07 が外れ、m08・m20 が当たる…）
./doc/UPSTREAM_DESIGN.md:419:    ⚠ 動くのは中央の腕だけで、反転した 4 題材はすべて判定の境目。

$ grep -rn "境目" --include=*.md . | grep -v acceptance
./.pair-agent/agreements/single-model-operation.md:88:…いずれも判定の境目（平均 p が 0.3 / 0.4 / 0.6 / 0.7 のすぐ近く）にある。
./.pair-agent/probes/single-model-1/report.md:104:…3 件は判定の境目だが、**m18 は境目ではない**…
./doc/UPSTREAM_DESIGN.md:419:    ⚠ 動くのは中央の腕だけで、反転した 4 題材はすべて判定の境目。
```

事実そのものは私も記録から引き直して確認した（`scratchpad/recount2.py`）:

```
C 反転: ['m03','m07','m08','m20']
  m03: s1 hit=True  p=0.333 -> s2 hit=False p=None（両読み手 NO_EVIDENCE/low_valid_rate 0.333/0.333）
  m07: s1 hit=True  p=0.300 -> s2 hit=False p=0.000（#b が NO_EVIDENCE・有効率 0.167）
  m08: s1 hit=False p=0.450 -> s2 hit=True  p=0.325
  m20: s1 hit=False p=0.667 -> s2 hit=True  p=None（両方 NO_EVIDENCE。想定 none に当たる）
B 反転の m18: s1 p=0.417 -> s2 両読み手 NO_EVIDENCE/low_valid_rate（有効率 0.333 と 0.167）で平均 p が無い
```

**どう直せば合格にできるか**（提案）

- 合意 `:87-88` を `report.md:103-104` と同じ文面に揃える（腕 C の 4 題材の入れ替え、m18 は境目でなく有効率の崩れ）
- 上流 `:419` も同様に。ついでに `:416` の「軸の出所と読み手だけ」も（下記 M-1）
- 対応表 `:171-172` の「直した」を、実際に直した後に書く

---

## major

### M-1 上流 §10:416 の「軸の出所と読み手だけを入れ替えた 2×2」が残る（3 回目 M-5 の後半）

**どこ**: `doc/UPSTREAM_DESIGN.md:416`
「**主に壊れるのは生成器。** **軸の出所と読み手だけを入れ替えた 2×2**（標本 1 ／ 標本 2）:」

**何が問題か**: 2 回目 M-1・3 回目 M-5 の指摘は「2×2 は軸の出所と読み手だけが違うのではない（閾値 κ・交差検証の有無・偽読み手）」。
`report.md:77` と `single_model_report.py:303` は「軸の出所と読み手を入れ替える」に直り、
さらに `report.md:79-83` に「腕の間で揃っていないものが 3 つある」の箇条書きが**正しく入った**（これは主張どおり）。
ところが上流だけ「**だけ**」が残っている。同じスプリントの 2 文書で言い方が食い違う。
3 回目 M-5 は「`report.md` に閾値の記述が無い」と「上流 `:416` の誤った言い方」の 2 つを含んでいたので、**半分しか閉じていない**。

**どう確かめたか**:

```
$ grep -rn "軸の出所と読み手" --include=*.md --include=*.py . | grep -v acceptance
./.pair-agent/probes/single-model-1/report.md:77:同じ題材・同じ判断規則で、軸の出所と読み手を入れ替える。…
./.pair-agent/probes/single-model-1/single_model_report.py:303:"同じ題材・同じ判断規則で、軸の出所と読み手を入れ替える。…"
./doc/UPSTREAM_DESIGN.md:416:  - **主に壊れるのは生成器。** 軸の出所と読み手だけを入れ替えた 2×2（標本 1 ／ 標本 2）:
```

### M-2 新しい「拒否権」の注意が、1 モデルでない既定の呼び方で出る。どの文書にも書かれていない

**どこ**

- `structural_distillation/compose.py:174-176`（`elif any(v.name == planner.name for v in verifiers) and len(verifiers) == 2:`）
- `doc/UPSTREAM_DESIGN.md:433`（「ライブラリは **1 モデルのとき**（交差検証なし・自己交差検証・読み手が全部同じモデル）判定に注意を出す」）
- `README.md:165`（「既定の検証役は偽でない読み手全員で、生成器を含むこともあります（**測定では生成器自身を検証役にしても検出数は同等でした**）」）

**何が問題か**

2 回目 m-5 への対応として足した注意だが、**既定の引数のまま読み手を 2 体渡すと、2 体が別モデルでも必ず出る**。
`planner` を省くと `resolved_planner = real[0]`、`verifiers` を省くと `resolved_verifiers = real` なので、
生成器は必ず検証役 2 体の 1 体になるからである。つまり

```python
judge(text, prop, out, readers=[gpt_x, claude_y])   # 別モデル 2 体・README が薦める形
```

が `Judgment.notes` を 1 件返し、`log.warning` も出す。**3 回目まではこの構成の注意は 0 件だった**
（`e2fadba` の `_note_models` は `generated and planner is not None` を要求し、既定では `planner=None` だった）。

注意の中身（「向きを外すには全員一致が要る」）は `l1.py:174` の `if opposing * 2 > len(verifiers)` と照らして**正しい**
（検証役 2 体なら 2 票そろわないと外れない。3 体なら 2 票で外れるので拒否権は無く、実際 `len(verifiers) == 2` に限っている）。
問題は次の 3 点:

1. **上流 §10:433 は「ライブラリは 1 モデルのとき…注意を出す」と枠を決めている**。多モデルの既定呼び出しで出る注意は
   この枠の外で、README にも上流にも 1 行も無い
2. **README:165 は同じ構成を「測定では検出数は同等でした」と安心させる**（根拠は上流 `:243` の「生成器自身を検証役にしても
   1/126・6/66（検出の数は同等）」）。ライブラリの注意だけが警告し、README は大丈夫と言う
3. 同じ 1 モデル構成では `elif` に入るので **自己交差検証の注意が出て拒否権は出ない**。
   拒否権は 1 モデルのときにも成り立つ事実なのに、1 モデルのときだけ黙る

**どう確かめたか**

```
$ .venv/Scripts/python .../scratchpad/chk_default.py
=== judge(text, prop, out, readers=[別モデル a, b]) 既定 Budget ===
  ・ 生成器 gpt-x が検証役 2 体の 1 体を兼ねている。向きを外すには全員一致が要るので、生成器は自分が作った軸に拒否権を持つ
  notes 件数: 1
```

`Budget.crosscheck` の既定は `True`（`contracts.py:136`）なので、追加の引数は要らない。

**どう直せば合格にできるか**（提案）: README と上流 §10 に「検証役 2 体の 1 体が生成器なら拒否権になる」を 1 行足し、
README:165 の「検出数は同等」と並べて読めるようにする。あるいは注意の文面に測定値（1/126・6/66）を添える。

### M-3 3 回目の minor 11 件の「対応／見送り」がどこにも記録されていない（3 回目 M-8 と同じ形）

**どこ**: `.pair-agent/agreements/single-model-operation.md:162-177`（3 回目の対応表）、
`.pair-agent/sprints/43898a8f-fb98-4ace-a6d3-dc6b1446d0ba.json` の 2026-09-23T19:30 の経過

**何が問題か**: 3 回目の対応表は C-1・C-2・major 1〜7・「m-1〜m-6（**2 回目の** minor）」の 10 行で完結している。
**3 回目が出した 11 件の minor（m-1〜m-11）についての行は 1 つも無い**。ジャーナルの経過にも「minor」の語が無い。
現物を突き合わせると、11 件のうち**閉じたのは 2 件だけ**（下記 minor の表）で、残り 9 件は手つかずである。

3 回目 M-8 が挙げた指摘はまさに「見送りなら見送りと 1 行書くべき」だった。
2 回目の minor についてはその行が入った（M-8 は閉じた）が、**同じ回でもらった 3 回目の minor には同じことが繰り返されている**。
依頼文の「対応したものと見送ったものがあります」を裏づける記録がリポジトリのどこにも無い。

**どう確かめたか**:

```
$ grep -n "3 回目\|minor\|見送" .pair-agent/agreements/single-model-operation.md
128:…不合格（critical 4・major 13・minor 10）…            （1 回目の話）
162:## 受入検査 3 回目（不合格・critical 2）と対応
177:| m-1〜m-6（2 回目の minor） | … | すべて直した。…|
$ python -c "…経過を全件読む…"   # 2026-09-23T19:30 の経過に「minor」の語なし
```

---

## minor

### 3 回目の minor 11 件の現状（この表が M-3 の実体）

| 3 回目 | どこ | 現状 | 判定 |
|---|---|---|---|
| **m-1** `threshold_sweep.py` が C の標本 2 の行を出さない | `w5/threshold_sweep.py` | 今回も 5 行だけ（B s1・B s2・C s1・A s1・A s2）。C s2 の升目は閾値を入れ替えて検めていない | **閉じていない** |
| **m-2** `A2 = 16` を定数で埋めている | `single_model_report.py:335` | `A2 = 16` のまま。報告はこの升目を自分では数えない | **閉じていない** |
| **m-3** 読み手の寄与の符号が報告の中で逆向き | `report.md:92`「読み手の寄与（**B − A**）: 想定一致 **-1** 件」 対 `:98` 表「読み手の寄与 **A−B**」で **+1** | そのまま | **閉じていない** |
| **m-4** `planner="mechanical"` の問いに無意味な注意 | `single_model_run.py:105` の検出項目 | いまも出る（私の台本でも「渡された問い・交差検証なし・読み手 2 体同モデル」で `この問いの集合を作った生成器は …・交差検証なし（記録に残った名前）` が出る）。文面に「・交差検証なし」が増えた分、むしろ長くなった | **閉じていない** |
| **m-5** 偽読み手だけの構成で注意 0 件 | `compose.py:163-164` | **閉じた**。`偽読み手しかいない（較正だけの走行）。判定の値は返らない` が出る。私も `summary.representative is None` を確認 | **閉じた** |
| **m-6** 名前とモデル名を混ぜて誤断定 | `compose.py:183-184` | **半分だけ閉じた**（下記 m-12） | **部分** |
| **m-7** 引き直しと `report.md` が末尾の空行 1 行だけ違う | — | `143d142 <` のまま | **閉じていない** |
| **m-8** 合意 `:148` の「pytest 205」 | `合意:148` | 205 のまま。2 回目の報告書が記録した数は 204 で、205 は 3 回目に 1 件足した後の数。いま HEAD は 207 なので二重にずれた | **閉じていない** |
| **m-9** 合意 `:95` の評価語「かなり使える」 | `合意:95` | そのまま（同じ箇条書きの後半が打ち消している構造も同じ） | **閉じていない** |
| **m-10** 「交差検証の有無も効かない」は A・B 側だけの確認 | `合意:92-93` | 合意はそのまま。⚠ `report.md:81-82` には「C は自己交差検証あり（4 軸を外した）、A・B は無し」と片側だけの確認と分かる書き方が**入った**ので、報告側は改善 | **合意は閉じていない** |
| **m-11** `c_samples2/run.log` の先頭が `[m01] 済み` | `c_samples2/run.log:1` | そのまま | **閉じていない** |

### そのほか

- **m-12**（m-6 の残り）: 再利用・受け渡しの「生成器も読み手も同じモデル」は、**記録の生成器の「名前」が読み手の「モデル」名と
  一致すると、実体が別モデルでも言い切る**。生成器を `ScriptedReader("qwen:4b", model="m9")` で作った問いの集合を、
  モデル `qwen:4b` の読み手 2 体（名前は `x1`・`x2`）に渡すと
  「生成器も読み手も同じモデル（qwen:4b）…⚠ この判定では問いを作っていない（再利用）」と出る（実際は別モデル）。
  テストが当てているのは**逆の衝突**（記録の生成器名 ＝ 読み手の**名前**）だけ。
  さらに `compose.py:183-184` の `(qs.planner in models and any(model_of(r) == qs.planner for r in real))` は
  **2 つの条件が論理的に同値**（`models` は `{model_of(r) for r in real}` なので）で、後半は死んでいる。
- **m-13**: `compose.py:155` のドックストリングは「強いモデルが作った軸なら **15/18** だった」と無条件のまま。
  実際に出す注意は `:188` で「13〜15/18。標本数で動く」に直っている。同じ関数の中で 2 通り。
- **m-14**: `doc/UPSTREAM_DESIGN.md:525`「同じ弱い読み手は強い軸なら **15/18** を当てた」も無条件のまま
  （`README.md:161` は「13〜15/18（標本数で変わります）」に直っている）。3 回目 major 6 の直しが上流に届いていない。
- **m-15**: 注意の文面「想定に合った題材が**7/18** まで落ちた」は、文字列連結で空白が落ちたまま（`compose.py:187-188`）。
  3 回目 M-6 が「ついでに」として挙げた箇所。
- **m-16**: 上流 `:415`「p の中央値 **0.83**」に定義が無いまま。`report.md:45` には
  「題材ごとの平均 p で 0.833・読み手ごとの p で 0.817」が**入った**（2 回目 m-2 の対応）ので、上流だけ取り残された。
- **m-17**（新規・変異試験の担保）: 今回足した変異体のうち「既定の生成器を注意の対象にしない」
  （`tools/mutation_check.py:61-62`）と「既定の検証役を注意の対象にしない」（同 `:64-65`）は、
  **注意だけが消える状態を作っていない**。`resolved_planner = planner` は `planner` 省略時に
  `InputError("生成器が要る…")` を投げて**生成そのものを壊す**し、`resolved_verifiers = []` は
  **交差検証そのものを消す**。どちらも 3 回目 C-1 の退行（生成は正しく解決され、注意にだけ古い引数が渡る）とは別の壊れ方で、
  `test_default_planner_and_verifiers_are_the_real_readers` が別の理由で落とす「安い殺し」になっている。
  ⚠ ただし**実害は無い**。3 回目の退行そのものの形を私が変異体にして走らせると、新しいテストがきちんと殺した（下記）。
  変異体の**名前が担保している内容とずれている**という指摘。

---

## 特に検めてほしかった 4 点（依頼文）への回答

### ① `_note_models` と `judge` の書き換えで前の経路が壊れていないか（37 構成の総当たり）

`ScriptedReader` で自分で走らせた一覧（LLM 呼び出し 0）。
略号 ＝ XC無・自己XC・拒否権・読1体・読同一・生読同一・名前告知・偽のみ・保存差。

| 構成 | 出た注意 | 判定 |
|---|---|---|
| **G** 読み手2(同)・生成器省略・検証役省略 | 自己XC・読同一・生読同一 | **3 件。3 回目 C-1 は閉じた** |
| G 読み手2(同)・生成器渡す・検証役省略 | 自己XC・読同一・生読同一 | 過不足なし |
| G 読み手2(同)・生成器省略・検証役2渡す | 自己XC・読同一・生読同一 | 過不足なし |
| G 読み手2(同)・生成器渡す・検証役2渡す | 自己XC・読同一・生読同一 | 過不足なし（旧版と同じ） |
| G 読み手2(同)・生成器渡す・検証役1渡す | XC無・読同一・生読同一 | 正しい（2 体未満なので交差検証なし） |
| G 読み手1(実)・省略/省略 | XC無・読1体・生読同一 | 正しい |
| G 読み手1(実)・生成器渡す・検証役2渡す | 自己XC・読1体・生読同一 | 正しい |
| G 読み手2(**異**)・省略/省略 | **拒否権** | 正しいが未文書化（M-2） |
| G 読み手2(異)・生成器渡す(第3のモデル)・検証役省略 | （注意なし） | 正しい |
| G 読み手2(同)+偽1／+偽2／偽が先頭 | 自己XC・読同一・生読同一 | 偽読み手を正しく除いて既定を解決（先頭が偽でも `real[0]`） |
| G 読み手1(実)+偽1 | XC無・読1体・生読同一 | 正しい |
| G 偽のみ | `InputError: 生成器が要る` | 正しい |
| G 生成器が検証役2の1体を兼ねる（読み手は別モデル） | 拒否権 | 正しい |
| G 生成器が検証役**3**の1体を兼ねる | （注意なし） | **正しい**（3 体なら 2 票で外れるので拒否権は無い。`l1.py:174` と照合） |
| G 読み手2(同)・検証役だけ別モデル2体 | 読同一・生読同一 | 正しい（自己交差検証ではない） |
| G 交差検証OFF(budget) | 読同一・生読同一 | 既定どおり（`budget.crosscheck` で黙る。従来仕様） |
| **S** 再利用・読み手2(同)・生成器名=モデル名 | 読同一・生読同一・保存差 | 正しい |
| S 再利用・読み手2(同)・別系統の生成器名 | 読同一・**名前告知**・保存差 | **3 回目 C-2 の直し。検証役の名前も並ぶ** |
| S 再利用・読み手1(実) | 読1体・名前告知・保存差 | 正しい |
| S 再利用・読み手2(異) | 保存差のみ | 正しい（言えることが無い） |
| S 再利用・読み手2(同)+偽1 | 読同一・名前告知・保存差 | 正しい |
| S 再利用・偽のみ／偽1体 | **偽のみ** | **2 回目 m-5 の穴が閉じた** |
| S 再利用・検証役2渡す | 読同一・名前告知・保存差 | 正しい |
| **D** 渡す・読み手2(同)・生成器名=モデル名 | 読同一・生読同一 | 正しい |
| D 渡す・読み手2(同)・別系統 | 読同一・名前告知 | 正しい |
| D 渡す・読み手1(実) | 読1体・名前告知 | 正しい |
| D 渡す・読み手2(異) | （注意なし） | 正しい |
| D 渡す・読み手2(同)+偽2 | 読同一・名前告知 | 正しい |
| D 渡す・偽のみ2体／偽1体 | 偽のみ | 正しい（`summary.representative is None` も確認） |
| D 渡す・交差検証なしの問い・読み手2(同) | XC無・読同一・名前告知 | 正しい（注意は 1 度だけ） |
| D 渡す・交差検証なしの問い・読み手1 | XC無・読1体・名前告知 | 正しい |
| D 渡す・名前衝突（記録の生成器名 ＝ 読み手の**名前**・実体別） | 読同一・名前告知 | **正しく言い切らない**（新しいテストが当てている所） |
| D／S 名前衝突（記録の生成器名 ＝ 読み手の**モデル**名・実体別） | 読同一・**生読同一** | **誤断定が残る**（m-12） |

**結論: 3 回目と同じ形の退行は無い**。既定の呼び方で注意が消える穴は塞がり、
保存庫・受け渡し・偽読み手混在・検証役 1/2/3 体のいずれでも過不足は見当たらない。
新しく増えた注意は「偽読み手しかいない」（穴を塞ぐ・正しい）と「拒否権」（正しいが未文書化・M-2）と
「名前告知に検証役を並べる」（3 回目 C-2 の直し・正しい）の 3 つ。

### ② 追加した 4 個の変異体は意味のある穴を突いているか

| 変異体 | 殺したテスト | 評価 |
|---|---|---|
| 既定の生成器を注意の対象にしない | `test_default_planner_and_verifiers_are_the_real_readers` ＋ `test_notes_do_not_need_the_generator_to_be_named` | **安い殺し**。生成そのものが `InputError` になるので退行の形を模していない（m-17） |
| 既定の検証役を注意の対象にしない | 上記 ＋ `test_missing_or_self_crosscheck_is_reported`（3 件落ちる） | **安い殺し**。交差検証そのものが消える（m-17） |
| 偽読み手だけの走行を黙って済ませる | `test_a_reused_question_set_reports_its_recorded_origin` | **有効**。1 件だけ落ちる |
| 名前だけの一致で同じモデルと言い切る | `test_a_reused_question_set_reports_its_recorded_origin` | **有効**。1 件だけ落ちる |

**ただしテストは本物の退行を捕まえる。** 3 回目の退行そのものの形を自分で変異体にして走らせた:

```
$ .venv/Scripts/python .../scratchpad/mut_who.py
R3 3 回目の退行を再現（_note_models に list(verifiers)…else real と生の planner を渡す）-> 殺した
    FAILED tests/test_judge.py::test_notes_do_not_need_the_generator_to_be_named
    1 failed, 206 passed, 1 skipped
（生の planner だけ／生の verifiers だけ、の 2 通りも同じテストが殺した）
```

＝ **「テストの偶然」ではない**。落ちるのは新しいテスト 1 件だけで、しかもそれだけが落ちる。
変異体の名前と中身のずれ（m-17）は担保の記述の問題であって、退行の検出力そのものは足りている。

### ③ 作り直した 3 つの記録は測定値不変か・`single_model.json` を上書きしていないか

`git show e2fadba:<各 records.jsonl>` と HEAD を突き合わせ、全レコードを構造で比較した（`scratchpad/cmp_records.py`・`deep.py`）:

| 記録 | 件数 | `readings` | `summary` | `question_set` | `input` | 実際に変わったもの |
|---|---|---|---|---|---|---|
| `records.jsonl`（W1） | 21 | **21/21 一致** | 21/21 | 21/21 | 21/21 | `at` のみ（回答も `cached` も不変） |
| `c_samples2/records.jsonl` | 21 | **21/21 一致** | 21/21 | 21/21 | 21/21 | `at`・`cost`・`answers[].cached` **488 件**・`notes`（1 → 2 件） |
| `w5/records.jsonl` | 42 | **42/42 一致** | 42/42 | 42/42 | 42/42 | `at`・`cost`（21/42）・`answers[].cached` **1008 件**・`notes`（1 → 2 件） |

`matrices` の差分を 1 フィールドずつ数えると **`answer.cached` だけ**（c2 は 2440 回答中 488、w5 は 1512 中 1008。
`verdict`・`evidence`・`valid`・`raw` は 0 件）。**測定値は不変**。

`cost` はすべて `live={'plan':0,'crosscheck':0,'answer':0}` ＝ **実呼び出し 0**（主張どおり）。

`single_model.json` の上書き: `git log --oneline -- <path>` で
`single-model-1/single_model.json` は `0956a5f` 以来、`c_samples2/single_model.json` は `f551811` 以来**動いていない**。
`git diff --stat e2fadba HEAD -- .pair-agent/probes/` にも現れない。作業ツリーも `git status --porcelain` が空。
probe `README.md:51` の「⚠ `single_model.json` は走行時のまま」も**主張どおり**。

### ④ 5 文書の数字と言い回しの食い違い

| 数字・言い回し | 合意 | `report.md` | 上流 | README | probe README | 判定 |
|---|---|---|---|---|---|---|
| 非排他 **5.6 倍** | :115・:138・:156 ○ | :68 ○（6.6 は「分子と分母で検証役が変わる」と注記つき） | :433・:517 ○ | — | — | **揃っている**（0.135/0.024 = 5.62 を自分で再計算） |
| **13〜15/18** | :79・:95（表と本文で 15/13 を併記）○ | :88・:100（標本 1 の表なので 15/18）○ | :417 ○ / **:525 ✗ 無条件 15/18** | :161 ○ | :35 ○ | **上流 :525 が取り残し**（m-14）。`compose.py:155` のドックストリングも無条件（m-13） |
| 変異体 **64** | :173 ○（53→57→60→64） | — | — | — | — | **揃っている**（AST で 0956a5f=53・6344c91=57・089410b=60・HEAD=64 を再現。走行も 64・生き残り 0） |
| 腕 C の 4 題材入れ替え | **:87 ✗** | :103 ○ | **:419 ✗** | — | :35（数字のみ・誤りなし） | **C-1** |
| m18 は境目でない | **:88 ✗** | :104 ○ | **:419 ✗** | — | — | **C-1** |
| 「軸の出所と読み手**だけ**」 | — | :77 ○ | **:416 ✗** | — | — | **M-1** |
| 札 7/21 の内訳（p 同じ 4・p も動いた 3） | :122-123 ○ | — | :427 ○ | — | — | **主張どおり**（下記で検算） |
| `pytest` の数 | **:148「205」✗** | — | — | — | — | m-8（実測 207） |

### ライブラリの注意の文面が、いま支持されている根拠だけで書かれているか

| 注意 | 主張 | 根拠の裏 | 判定 |
|---|---|---|---|
| 偽読み手しかいない…判定の値は返らない | 値なし | `summary.representative is None`・`readers == []` を自分で確認 | **支持されている** |
| 向きの交差検証をしていない… | — | — | **支持されている** |
| 自己交差検証…外せた軸は他系統の 1/4 | 適合率 1.00・再現率 0.27（自己 4 軸・他系統 15 軸） | 合意 `:157`・上流 `:428` と整合 | **支持されている** |
| 生成器が検証役 2 体の 1 体を兼ねる…拒否権 | 「向きを外すには全員一致が要る」 | `l1.py:174` の `opposing * 2 > len(verifiers)` と照合。2 体なら 2 票必要・3 体なら 2 票で外れる（コードも `len == 2` に限っている） | **支持されている**（文書化が無い。M-2） |
| 読み手が 1 体／全部同じモデル | — | — | **支持されている** |
| 7/18 まで落ちた…強い軸なら 13〜15/18。標本数で動く | B s1 15/18・B s2 13/18 | 自分で数え直して一致 | **支持されている**（空白落ち m-15・ドックストリング m-13 は残る） |
| この問いの集合を作った生成器は…（記録に残った名前） | 名前しか分からない | 実装と一致 | **支持されている**（`mechanical` への偽陽性 m-4 は残る） |

---

## 3 回目の指摘の開閉（1 件ずつ）

| # | 3 回目の指摘 | 開閉 | 根拠 |
|---|---|---|---|
| **C-1**（退行） | 既定の呼び方で注意が 2 件消えた | **閉じた** | `judge(text, prop, out, readers=[a, b])`（同モデル 2 体）で 自己交差検証・読み手が全部同じ・生成器も読み手も同じ の**3 件**が出る。入口で `resolved_planner`・`resolved_verifiers` に 1 度だけ解決し、生成と注意で同じものを使う造り。**退行そのものの形を私が変異体にしても、新しいテスト 1 件がきちんと殺した**。37 構成の総当たりでも消えた注意は無い |
| **C-2**（未了） | 再利用経路の自己交差検証の条件が決して真にならない・テストが手組み | **閉じた** | `set(crosscheck.verifiers) == {qs.planner}` の枝は**消えた**。再利用では `この問いの集合を作った生成器は X・検証役は V1・V2（記録に残った名前）` を出す。テストから `replace(..., verifiers=["solo"])` の手組みが消え、`judge()` が実際に作った問いの集合だけで書かれている（`tests/test_judge.py:335-372`）。実走行と同じ `#plan`/`#v1`/`#v2` の構成でも名前が出ることを自分で再現。作り直した `c_samples2`・`w5` の記録 63 件すべてにこの注意が載っている |
| **major 1** | 腕 C も 4 題材入れ替わる | **閉じていない** | `report.md:103`・`single_model_report.py:345` は直った。**合意 `:87` と 上流 `:419` は未修正**。対応表 `:171` は「報告と合意を…直した」と書いている（**critical C-1**） |
| **major 2** | m18 は境目ではない | **閉じていない** | `report.md:104`・`single_model_report.py:348` は直った。**合意 `:88` と 上流 `:419` は未修正**（**critical C-1**） |
| **major 3** | 変異体の数が誤り | **閉じた** | 合意 `:173` が「53 → 57 → 60 → **64**（今回 +4）」。私の AST 数え直しと完全一致。走行も 64 個・生き残り 0。ジャーナルの「57 個」「60 個」も直っている |
| **major 4** | `report.md` に 6.6 倍が残る | **閉じた** | `report.md:68` が 5.6 倍。`single_model_report.py:248` も `x['cloud'] / STRONG_NONEXCLUSIVE`。6.6 は「分子と分母で検証役が変わる」と明記して併記。5 文書で 5.6 に揃った |
| **major 5** | `report.md` に閾値の話が無い／上流の言い方 | **部分** | `report.md:79-83` に「腕の間で揃っていないもの 3 つ（閾値・交差検証・偽読み手）」が正しく入った。だが**上流 `:416` の「軸の出所と読み手だけ」は残る**（**major M-1**） |
| **major 6** | 注意が標本依存の 15/18 を無条件に言う | **閉じた**（傷つき） | `compose.py:188` が「13〜15/18。標本数で動く」。⚠ 同関数のドックストリング `:155` と 上流 `:525` は無条件 15/18 のまま（m-13・m-14）。空白落ち「題材が7/18」も残る（m-15） |
| **major 7** | `w5/`・`c_samples2/` の記録が修正前製 | **閉じた** | 3 つとも実呼び出し 0 で作り直され、`readings`・`summary`・`question_set`・`input` が **21/21・21/21・42/42 で全件一致**。変わったのは `answers[].cached`・`cost`・`at`・`notes` だけ。`single_model.json` は未変更 |
| **2 回目 m-1** p は同じでも | **閉じた** | 合意 `:122-123`・上流 `:427` に内訳。私も `single_model.json` から数え直して **割れた 7 件 ＝ m01・m02・m07・m11・m14・m16・m20**、うち **p が同じ 4 件 ＝ m01・m02・m11・m14**、**p も動いた 3 件 ＝ m07・m16・m20** で完全一致 |
| **2 回目 m-2** p の中央値の定義 | **部分** | `report.md:45` に「題材ごとの平均 p で 0.833・読み手ごとの p で 0.817」が入った。**上流 `:415` は「p の中央値 0.83」のまま**（m-16） |
| **2 回目 m-3** 表の「—」の凡例 | **閉じた** | `report.md:16` に「値が無い（札が『本文に根拠が無い』か『計器不良』）という意味。腕を走らせていない、ではない」 |
| **2 回目 m-4** 弱率が飽和するのは OR | **閉じた** | `report.md:15` に `is_weak |= "NEITHER" in codes` の **OR** と「非排他は多数決なので飽和しない」。`l1.py:169` と照合して正しい |
| **2 回目 m-5** 生成器が検証役を兼ねると拒否権 | **閉じた**（副作用あり） | 注意として出る。文面は `l1.py:174` と照合して正しい。⚠ 既定の多モデル呼び出しで出るのに未文書化（**major M-2**） |
| **2 回目 m-6** `cached` の指す場所 | **閉じた** | probe `README.md:49` が「回答ごとのキャッシュ命中の印（`matrices[].answers[].cached`）」 |
| **3 回目 m-1〜m-11** | 11 件 | **2 件だけ閉じた**（m-5・m-6 は部分）。**9 件は手つかずで、対応／見送りの記録も無い**（**major M-3**。個票は上の minor 表） |

---

## 確かめた結果、主張どおりだったもの（沈黙で済ませない）

| 主張 | 自分で確かめた値 | 判定 |
|---|---|---|
| pytest 207 通る | `207 passed, 1 skipped` | **主張どおり** |
| 適合検査 21/21・21/21・11/11・外れ 0 | すべて一致・実呼び出し 0 | **主張どおり** |
| 変異体 64 個・生き残り 0 | `殺した` 64 行・`生き残り 0: []`。AST でも 64 | **主張どおり** |
| 変異体の数の履歴 53 → 57 → 60 → 64 | `0956a5f`=53・`6344c91`=57・`8fdc1a4`=57・`089410b`=60・`f551811`=60・`e2fadba`=60・`HEAD`=64 | **主張どおり** |
| 閾値（κ=0.667 と 0.5）で 2×2 が動かない | `threshold_sweep.py` の 5 行を再現。C 標本 2 も自分で両方の閾値で数えて 7/18 で不変 | **主張どおり**（記述より強い。行が無いのは m-1） |
| 2×2 の 6 升（16/15/7 と 16/13/7）・寄与 +8/+1 と +6/+3 | 自分で集計して全一致。R-g の規則で 標本 1「生成器」・標本 2「両方」 | **主張どおり** |
| 腕 C の 4 題材入れ替え（m03・m07 が外れ、m08・m20 が当たる） | 完全一致。m03 と m20 は標本 2 で平均 p が無くなる（両読み手 `NO_EVIDENCE/low_valid_rate`） | **主張どおり**（書く場所が足りない＝ C-1） |
| m18 は標本 2 で有効率 0.333／0.167 の崩れ | `w5.json` から再計算して一致 | **主張どおり** |
| 札が 7/21 で割れ、p が同じ 4 件・p も動いた 3 件 | m01・m02・m11・m14（p 同じ）／m07・m16・m20（p も動いた） | **主張どおり** |
| 非排他 自己 0.159（20/126）・雲 0.135（17/126）・強い軸 0.024（3/126）・5.6 倍 | `out4/xc2` から再計算して一致（0.1587・0.1349・0.0238・5.62） | **主張どおり** |
| 3 つの記録を実呼び出し 0 で作り直し・測定値不変 | `live` はすべて 0。`readings`/`summary`/`question_set`/`input` が全件一致。差は `answers[].cached` だけ | **主張どおり** |
| `single_model.json` を上書きしていない | `git log` で `0956a5f`／`f551811` 以来変更なし。作業ツリーも clean | **主張どおり** |
| `report.md` は `--run . --w5 w5 --c2 c_samples2` の出力 | 末尾の空行 1 行を除いて完全一致 | **ほぼ主張どおり**（m-7） |
| 既定の呼び方で 3 件の注意が出る（3 回目 C-1 の直し） | 同モデル 2 体・引数 2 つだけで 3 件。37 構成の総当たりでも欠けなし | **主張どおり** |
| 手組み `verifiers=["solo"]` がテストから消えた | `tests/test_judge.py` に `"solo"` は無く、`judge()` が作った問いの集合だけで当てている | **主張どおり** |
| 偽読み手だけの走行で注意が出る | `偽読み手しかいない（較正だけの走行）。判定の値は返らない`。`representative is None` も確認 | **主張どおり** |
| 検証役 3 体で拒否権を言わない | 言わない。`l1.py:174` に照らして正しい | **主張どおり** |
| `CLAUDE.md` と `GEMINI.md` の同期 | `diff` が空 | **主張どおり** |
| 1〜3 回目で確認済みの数字（R-a 7/18・R-d 適合率 1.00/再現率 0.27・p≥0.8 で想定が支持でない 6/18・他系統を借りても 7/18・弱率 0.944/0.849/0.698） | `report.md` を引き直して全一致 | **壊れていない** |

---

## 完了条件への当てはめ（合意 `:181-184`）

| 完了条件 | 状態 |
|---|---|
| 4 腕（＋W5・標本 2）の記録がリポジトリに取り込まれ、判断規則に照らした読みが合意に書かれている | **部分的**。記録・走行器・読み取り器はそろい `report.md` は引き直しでほぼ完全一致。だが**合意に書かれた読み（`:87-88`）が記録と食い違う**（C-1） |
| 上流設計に「1 モデルのとき何を保証し、何を保証しないか」が書かれている（§10 の適用限界） | **部分的**。`:433` の「1 モデルのときは注意が出る」は**既定の呼び方で成り立つようになった**（C-1 の直し）。だが `:416`・`:419`・`:525` に誤り・取り残しが残り、`:433` の枠は拒否権の注意を含んでいない |
| 実装で直すべきものが出たら、直すか、次の題として書く | **満たす**（3 回目の C-1・C-2 はどちらも実装で閉じた） |
| 受入で critical 0 | **満たさない**（critical 1） |

## 再受入で見るところ

1. **C-1**: 合意 `:87-88` と 上流 `:419` を `report.md:103-104` と同じ内容にしたか。
   対応表 `:171-172` の「報告と合意を直した」を、実際に直した状態にしたか
2. **M-1**: 上流 `:416` の「軸の出所と読み手**だけ**」を消したか
3. **M-2**: 拒否権の注意を README と上流 §10 に書いたか（README:165 の「検出数は同等」と並べて読めるように）。
   または注意の文面に測定値を添えたか
4. **M-3**: 3 回目の minor 11 件について、対応したか見送ったかを 1 行ずつ書いたか
5. **m-12**: 記録の生成器「名」が読み手の「モデル」名と一致したときに言い切る穴を塞いだか
   （`compose.py:183-184` の同値な 2 条件も整理したか）
6. **m-13・m-14・m-16**: `compose.py:155` のドックストリング・上流 `:525` の無条件 15/18・上流 `:415` の p の中央値の定義
7. **m-17**: 「既定の生成器／検証役」の変異体を、注意だけが消える形（`_note_models` の引数を生に戻す）に書き換えたか
